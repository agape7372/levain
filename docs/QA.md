# 르방이 — 수동 QA 체크리스트 (릴리스 게이트)

전제: vitest 전 suite green. 아래는 실기기/에뮬 수동 검증. 판정이 애매한 항목은 본 세션 재확인.

## A. 시간·시뮬레이션

- [ ] 강제종료 → 6시간 뒤 재실행: 상태가 경과만큼 진행, 문구·비주얼 정합(즉시 스냅 — 재생 애니메이션 없음)
- [ ] 기기 시계 하루 **뒤로**: 크래시 0, 전 타임스탬프 재정박(나이·단계 퇴행 없음), 알림 재예약
- [ ] 기기 시계 하루 **앞으로**: 정직한 catch-up (배고픔/시큼 진행)
- [ ] 10일 방치 시뮬(시계 조작): 휴면 도달(아직 곰팡이 아님), 부활 의식 2세션 완주 → 활발 복귀, maturity 보존.
  **14일(336h)은 곰팡이 사망 임계와 겹침 — G절에서 별도 검증**
- [ ] 급여 연타: maturity 미적립(유효 6h 게이트), mass·산미 정상, 착취 불가

## B. 저장

- [ ] 새로고침·강제종료 후 상태 보존 (액션 직후 동기 저장)
- [ ] 주 슬롯에 쓰레기 문자열 주입 → Preferences 미러 폴백 / 둘 다 손상 → 새 게임 + 담백한 안내
- [ ] NaN·범위 밖 값 주입 → clamp 회수(새 게임으로 안 튐)
- [ ] 설정 "기록 내보내기" → 파일 → "불러오기" 왕복 일치
- [ ] **앱 재설치 → Auto Backup 복원 상태 확인** (allowBackup — 복원되면 보너스, 안 되면 내보내기 안내가 정본)
- [ ] 굽기 연출 중 강제종료 → 재실행 시 pendingBake 결과 카드 재노출, 확인 연타에도 중복 기록 0

## C. 알림 (에뮬 + 실기기)

- [ ] 활발 상태에서 액션 직후 **슬롯 3개(밥·휴면·moldWarn) 전부 예약** 확인(`adb shell dumpsys notification`)
- [ ] 밥 직후 슬롯 1 예약 확인 → 시각에 수신 → 탭하면 앱 홈
- [ ] 냉장 이동 → 슬롯 1이 주간 반복 예약으로 전환, 앱 미실행에도 다음 주 발화. **moldWarn(슬롯 3)은 미포함** 확인
- [ ] 휴면 진입 후 moldWarn(슬롯 3) 1건만 남고 나머지 침묵 / reviveProgress=1이면 슬롯 1을 +8h로 재예약
- [ ] **곰팡이 사망 후 알림 0건**(사망 시점·사망 후 통지 없음 — G절 사망 시나리오와 함께 확인)
- [ ] 조용시간(22~08) 걸리면 다음 08:00으로 클램프
- [ ] 권한 거부 상태에서 전 기능 정상
- [ ] **권한 영구 거부(2회 거부) → 설정 화면 안내 + openSettings 딥링크 왕복**
- [ ] 기기 재부팅 → 예약 알림 생존(또는 resume 재예약으로 커버) 판정
- [ ] Doze/절전에서 inexact 지연 체감 — 문구("쯤")와 어긋나지 않는지
- [ ] manifest에 SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM 없음 확인 (`aapt dump permissions`)

## D. Android 내비게이션·시스템

- [ ] **하드웨어 백 전수**: 모달 열림→닫힘 / 레시피→홈 / 굽기 연출 중→스킵 / 루트→minimizeApp(종료 아님)
- [ ] Android 14+ predictive back에서 이상 동작 없음
- [ ] 세로 고정, 회전 시도에도 레이아웃 불변
- [ ] 상태바: 베이지 배경 위 **어두운 아이콘**, 펀치홀·엣지투엣지(targetSdk 35)에서 상단 HUD 안 가림
- [ ] 스플래시 단색 #E8D9C4 → 첫 화면 연속성(플래시 없음)
- [ ] OS 폰트 스케일 200%: 레이아웃 깨짐·잘림 없음
- [ ] 키보드(내보내기 파일명 등 입력 시) 겹침 없음

## E. 렌더·성능

- [ ] 레시피 탭↔홈 왕복: rAF 정지/재개 (GPU 프로파일로 확인)
- [ ] hidden(홈버튼) → rAF·오디오 정지, 복귀 → uTime 연속성(점프 없음)
- [ ] WebGL 컨텍스트 유실 강제(`WEBGL_lose_context`) → 자동 전체 재구축, 상태 보존
- [ ] 저사양 프로필(에뮬 swiftshader)에서 인터랙션 중 프레임 급락 없음
- [ ] draw call ≤ 10 확인(Spector.js 또는 renderer.info) — 홈 9(천 덮개 포함) / **쇼케이스 진입 시 별도 ≤4**
- [ ] DPR 2 기기에서 발열·배터리 이상 없음 (10분 방치)

### 물성 축 분해 · 유리 접촉 (2026-08-25)

`scripts/motionlab-shot.mjs --all <dir>`로 8프리셋 일괄 촬영 후 육안. 셰이더 컴파일 실패는 이 스크립트가 잡는다.

- [ ] **피크에서 반죽이 유리에 눌려 있는가** — 공처럼 뭉쳐 유리에서 떨어지면 실패(구 동작)
- [ ] **갓 밥준이 되직해 보이는가** — 묽은 배터로 읽히면 실패. 잡아 늘였다 놓으면 안 튕기고 자국이 남는다
- [ ] 유리벽에 기공 필드가 보이는가(피크·상승에서 가장 진하게), **세로 홈은 없는가**
- [ ] 어두운 원형 함몰·밀집 점 클러스터 0 (트라이포포비아 규칙 §2 — 밝은 볼록 돔만)
- [ ] 피크 정수리가 **잘게 부서진 크러스트**인가 — 소프트아이스크림 꼭지 하나면 실패
- [ ] 광택 순서: 피크 > 상승 > 갓 밥준 (갓 밥준은 무광이 정상 — 실사진 정합)
- [ ] 하강·배고픔·시큼에서 **최고 수위 자국이 현재 수면보다 위에** 보이는가
- [ ] 휴면·곰팡이에서 유리벽 기공이 0인가 (죽은 분기 게이트)
- [ ] 젓고 떼었을 때 전신 출렁임이 **2회 안쪽**에서 멎는가 (구 동작은 5~6회 = 물)

## F. 콘텐츠·UI

- [ ] 온보딩(창조 의식) 완주 → 재실행 시 스킵
- [ ] 성장 단계 0→2 압축 시뮬(시계 조작): D2 가짜 부풀기 이벤트, 잠잠기 억제, 첫 해금 토스트
- [ ] 레시피 탭: 첫날부터 실루엣 도감(빈 탭 아님), 해금 조건 문구, 해금 시 wipe 연출
- [ ] 레시피 그리드: 썸네일(`public/breads/thumbs/`) 미생성 빵은 breadArt 폴백 렌더 확인
- [ ] 띄워보기: activity 경계(0.7)에서 문구 전환
- [ ] 굽기: mass 게이트(비용+60g 미달 시 진입 불가 + 담백한 안내), 등급 3종 각각 재현
- [ ] 호밀빵: 시큼 상태(산미 40~75)에서 고득점 확인
- [ ] **전 모달 중앙 팝업**(바닥 시트 0건), 전 문구 시스템어 0건 (copy.ts 전수 대조)
- [ ] **모달 열림 중 탭바 딤·차단** (BUG-2 회귀): 아무 확인 모달이나 열고 탭바 확인 — 백드롭이 탭바를
  덮어 어둡고, 탭을 눌러도 반응 0 (백드롭 탭으로만 닫힘)
- [ ] 사운드 5종 재생·음소거 토글, 첫 제스처 전 무음(콘솔 경고 0)
- [ ] 햅틱 3종 발화, 설정 오프 시 전부 무진동

## H. 멀티 르방·확장 (저장 v2 이후)

- [ ] 스와이프·칩 ‹ › 전환 시 캔버스가 방향대로 밀려나갔다 돌아오는가 (교체는 화면 밖에서)
- [ ] 연타해도 애니메이션이 입력을 삼키지 않는가 (진행 중이면 즉시 컷으로 전환)
- [ ] 시스템 '모션 줄이기' 켜면 전환이 즉시 컷으로 바뀌는가
- [ ] 르방마다 덩어리 생김새·숨 박자가 다른가 (`uSeed` = createdAt — 앱 재시작해도 같은 자리)
- [ ] 이름 칩이 전환 방향에 맞춰 교체되는가

- [ ] v1 저장본 업그레이드: 구버전 설치 → 새 버전 → 이름(칩)·도감 배지·flake 무손실, 새 게임으로 안 튐
- [ ] 멀티(Levain Lab로 2~3마리 설치): 칩 ‹ › 전환 — 씬 즉시 스냅·반점 자리 개체별 고정, 브리핑 카드 안 뜸
- [ ] 알림 병합: 2마리 활발 상태에서 예약 확인 — **슬롯 3개 유지**(N배 아님), 집계 문구("르방이 2개가…")
- [ ] 비율 모달 "냉장 갈 준비"(3단계+·냉장 밖에서만 노출): 1탭에 밥+냉장 이동, 세그먼트 냉장 활성 확인
- [ ] 관찰 카드 부풀기: 범위 표기("N~M 뒤쯤"), 피크 구간 중엔 "지금이 한창때예요"
- [ ] 스와이프 전환(§5-5): 배경에서 좌우 48px+ 드래그 = 전환(왼쪽 = 다음, 칩 › 와 동일) /
      반죽 위 드래그는 grab·stir 그대로(불간섭) / 1마리·모달·쇼케이스·급여 연출 중 무효 /
      30px 미달·대각선(수평 1.5배 미만)은 아무 일 없음 / 칩 ‹ › 버튼으로도 동일 도달
- [ ] 밀가루 축(Phase 4): 비율 모달 "자세히"(4단계+) — 호밀 선택 시 피크 예보 단축 확인 /
      호밀 급여 후 다음 배고픔 알림·예측이 다목적보다 이르다 / 색 톤(통밀 담갈·호밀 회갈) /
      구버전 저장 로드 시 flour=다목적으로 무손실
- [ ] 재료·변형(Phase 6, 2026-08-24 레시피/도감 UX 재편 반영 — 상세 화면 폐지):
      **완성 카드 탭 = 바로 3D 쇼케이스**(GLB 없으면 카드 폴백) + 하단 "다시 만들기" 버튼 —
      단 감상 진입(완성 카드 탭)일 때만 노출, 굽기 결과 연출로 들어온 쇼케이스엔 없음 /
      **미완성·discard 카드 탭 = 굽기 모달 재료 옵션**: "기본 {빵}" + 발견한 변형(무소비 재굽기,
      mass만) + 미발견&재료 보유 변형("{재료} 1개를 넣어요") 목록 노출, 미발견&재고 0은 목록에
      없음(시도 전 차단, 소비 0) / 첫 발견 = 재료 1 소비 + 도감 발견 원자 커밋, 재굽기는 재소비 없음 /
      discard 베이스 변형(예: 초코칩 팬케이크)도 같은 모달에서 뜨고 bakeDiscard 경로(쿨다운 동일,
      판정 없음)를 타는지 /
      **도감**: 세그먼트 `레시피 | 도감`(하위 토글 `빵 | 재료`) 두 줄이 경계 없이 이어붙는지, 재탭
      = 레시피↔도감 토글(힌트 토스트 "…도감이 열려요") / 도감-빵 발견 = 아트+이름만(등급·횟수 메타
      없음), 미발견 = ?-실루엣(단색화+중앙 물음표) 탭 시 힌트 토스트만 / 도감-재료 "밝혀짐" 판정 =
      보유>0 또는 그 재료 쓴 발견 변형 존재, 아니면 ?-실루엣 /
      **르방 추가**: 홈 + 버튼(항상 노출) → 확인 모달 → 자동 이름 "르방이 N" 생성, 슬롯 3 초과 시
      "병이 세 개까지예요" 토스트, 이름 짓기 5단계 게이트는 그대로인지

## G. 신규 시스템 (곰팡이·플레이크·브리핑·천 덮개·촉감·쇼케이스)

**QA 노트**: 반점(168h)·확산(240h)·사망(336h) 임계는 시계 조작만으로 정밀 재현이 어려우므로
`levain:save`의 `sim.lastFedAt`을 직접 조작(현재시각 − 170h / − 250h / − 340h)한 뒤 새로고침으로 재현한다.

- [ ] `lastFedAt` −170h → 관찰 카드 '표면' 행에 반점 표시 + 데드라인 문구, moldWarn 알림 예약 확인
- [ ] −250h → 확산 표시로 갱신 / −340h → **곰팡이 사망 모달 자동 노출**(앱 열 때마다 재노출)
- [ ] **사망 후 (a) 플레이크 有** → "말린 조각으로 다시 시작" → maturity 0.6배 축소 → 12h 경과(저장 조작) →
  2회차 급여 → revived·활발 복귀
- [ ] **사망 후 (b) 플레이크 無** → "새 반죽으로 시작" → 새 개체(maturity 0, label 초기화), 도감 보존 확인
- [ ] 말리기 버튼: 3단계 미만 숨김 / 3단계 이상 & phase≠활발 비활성 / 3단계·활발·mass≥80 → 확인 모달 →
  성공 시 mass −20g + 관찰 카드 '보관' 행 갱신
- [ ] 복귀 브리핑: 부재 8h 이상 후 열기 → "그동안 있었던 일" 시간순 모달 / 8h 미만은 안 뜸
- [ ] 천 덮개: 콜드 스타트·hidden 30분+ 복귀 시 덮인 상태로 시작, 위 플릭/탭 → 걷기 + 사락 + 밀가루 모트,
  덮인 동안 poke·젓기 무시
- [ ] 젓기 촉감: **빠른** 드래그 → 소용돌이 시어장 + 끈적한 실 능선 + 기포 가속 + squelch, 손 떼면 spring 복귀,
  **세션(down~up)당 햅틱 최대 1회**
- [ ] **grab 촉감** (Phase 1 신규): 반죽 위에서 **천천히** 끌기 → 잡은 점이 손가락을 따라오고 목이
  가늘어지며 얇은 부분이 밝아짐 / **당긴 채 멈춰도 변형 유지**(소멸 0) / 놓으면 즉시 안 튕기고
  일부(잔류)가 0.8~1.4s 남았다가 천천히 처짐, 오버슈트 체감 0 / 반죽 밖에서 시작한 드래그는 grab 안 됨
- [ ] grab 상태 물성: 배고픔에서 놓으면 잔류가 더 크고(처짐), 휴면은 조금만 늘어나고 뻣뻣 —
  프리셋 간 차이가 체감되는지 (`motionlab.html` 프리셋 스와이프로 비교)
- [ ] 기포 순화: 팝 순간 깊은 함몰 없음(얕게 −0.12) + 밝은 링 잔상, 동시 가시 기포 ≤4
- [ ] 탭(poke): 가우시안 덴트 펄스 + spring 복귀 + 근처 기포 유도 / 롱프레스(0.8s+): 표면이 천천히 부풂, 떼면 easeOut
- [ ] 쇼케이스: 굽기 결과 — GLB 있는 빵은 턴테이블+드래그 회전+김 파티클, 백 버튼 → 홈 복원 /
  GLB 없는 빵은 기존 카드 결과로 폴백
- [ ] **쇼케이스 중 탭 전환** (BUG-1 회귀): 쇼케이스 열린 채 르방이 탭 → 쇼케이스 정리·홈 정상 →
  레시피 탭 복귀 → **카드 그리드 정상 렌더(백지 0)**, 이후 굽기·쇼케이스 재진입도 정상

## I. 무료 경제 (Phase 7 — GDD §6-5)

**QA 노트**: 잔액은 파생이라 저장에 없다. 확인하려면 `levain:save`의 `shared.economy`
(feeds·bakes·stageMax·exchanged·spent·gifted)를 보고 배너 숫자와 대조한다.
`tests/economy.test.ts`가 산술·수용 기준을 덮으므로 여기선 **표면과 체감**만 본다.

- [ ] 첫 재료 선물: 새 게임 → 창조 의식 직후 "첫 재료를 하나 골라요" 모달 → 고르면 토스트 +
  재료 탭에서 그 재료 1개 보유. **다시 뜨지 않음**
- [ ] 기존 저장본(1.2.3에서 이어받기): 첫 실행 시 브리핑 뒤 같은 모달 / 곰팡이 사망 모달이 뜬
  경우엔 **얹히지 않고** 재료 탭 배너의 "첫 재료를 하나 골라요" 버튼으로 받을 수 있는지
- [ ] 배너: 도감 → **재료** 탭에서만 보인다(레시피·빵 탭에는 없음). 세그↔하위줄 간격 0,
  하위줄↔배너·배너↔그리드 각 14px — 1.2.2 확정 간격이 그대로인지
- [ ] 교환소: [재료 바꾸기] → 4종 전부 이름·보유 수 표시 / 가루 12개로 가져오기 → 잔액 −12·재료 +1·토스트 /
  잔액 12 미만이면 버튼 비활성 / 보유 0이면 "가루 4개로 바꾸기" 비활성
- [ ] 소프트캡: 한 재료를 9개까지 채운 뒤 → 구매 버튼 비활성, 눌러도 **가루 무차감** +
  "9개까지 둘 수 있어요" 토스트 (개발자 모드 "재료 전부 +9"로 재현)
- [ ] 미션: [가루 모으기] → 4줄(밥 주기·빵 굽기·성장·처음 만든 빵). **기한·연속·리셋 문구가
  한 줄도 없는지** — 누적 횟수만 말해야 한다
- [ ] 보상 체감: 밥 주기를 10회 채우면 "가루 12개가 생겼어요" 토스트 + 배너 숫자 갱신
- [ ] 성장 보상 소급: 이미 5단계인 저장본을 열면 배너가 가루 60에서 시작하는지(단계 12×5)
- [ ] 롤백 안전: 1.2.3 번들로 되돌린 뒤 저장이 열리고 재료·도감이 남는지 (economy 키만 무시됨)
