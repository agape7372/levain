# 르방이 — 게임 디자인 문서 (GDD)

> **정본 선언**: 게임 규칙·수치·콘텐츠는 이 문서가 정본이다.
> 씬·uniform은 [VISUAL.md](VISUAL.md), 코드 계약·구조는 [ARCHITECTURE.md](ARCHITECTURE.md)가 정본.
> 세 문서가 충돌하면 각자의 관할 영역이 이긴다. 원문 설계·비판은 `design/` 참조(정본 아님).

## 0. 제품 한 줄

실제 사워도우 스타터(르방)의 리듬 — 하루 1~2번 밥, 4~8시간 피크, 냉장하면 주 1회 — 을
그대로 옮긴 **실시간 다마고치**. 키워서(성장), 해금하고(레시피), 떼어 굽는다(수확).
무캐릭터·코지 톤·액션으로는 죽지 않음·완전 로컬.
실패는 하나뿐이고 예고된다 — 곰팡이. 말린 조각으로 대비할 수 있다.

## 1. 시뮬레이션 패러다임: 닫힌 함수 모델

저장 상태는 **타임스탬프 + 느린 누적값만**. 활성도·부피·단계·냄새·배고픔은 전부
`유효 경과시간 = (now − lastFedAt) × 온도배율`의 닫힌 함수로 파생한다.

- 오프라인 catch-up이 O(1) — 틱 루프·고정 스텝·remainder 없음.
- 위치(온도)는 앱이 켜져 있을 때만 바뀌므로 오프라인 구간은 항상 단일 배율.
- sim 안에 확률 이벤트 없음 — PRNG 없음. 렌더 화장품 랜덤은 render 층 `Math.random`.
- 파생 가능한 값은 절대 저장하지 않는다.

## 2. 저장 상태 (SimState)

| 필드 | 타입 | 의미 |
|---|---|---|
| `createdAt` | epoch ms | 탄생 시각. 성장 단계의 "최소 일수" 게이트 |
| `lastFedAt` | epoch ms | 마지막 급여. 활성 곡선의 원점 |
| `lastSimulatedAt` | epoch ms | 마지막 반영 시각. 시계 역행 방어 기준 |
| `feedRatio` | `'1:1:1' \| '1:2:2' \| '1:5:5'` | 마지막 급여 비율 — 곡선 시간축 결정 |
| `flour` | `'white' \| 'wholewheat' \| 'rye'` | 마지막 급여의 밀가루(§3-1-1). 급여에서만 바뀜, 구세이브 키 부재 → white |
| `location` | `'room' \| 'window' \| 'fridge'` | 병 위치 = 온도 배율 |
| `acidity` | float 0~100 | 산미 누적 |
| `maturity` | int ≥0 | 건강 급여 사이클 수 — 성장·해금 축 |
| `mass` | int 60~480 (g) | 병 속 총량 — 굽기 경제 축 |
| `reviveProgress` | `0 \| 1` | 부활 의식: 0=정상, 1=휴면 중 1회차 급여 완료(2회차 대기). 완료 시 0 복귀 |
| `locAnchorAt` | epoch ms | 위치 앵커 — 마지막 급여 또는 위치 변경 시각 |
| `effBaseMs` | ms | 위치 변경 전까지 접어 둔 유효시간. `유효경과 = effBaseMs + (now−locAnchorAt)×배율` — 세션 중 위치를 바꿔도 과거 구간이 새 배율로 재해석되지 않게 하는 회계 |
| `lastDiscardBakeAt` | epoch ms \| null | discard 레시피 "급여당 1회" 쿨다운 비교용 |
| `flake` | `{ madeAt: number; maturity: number } \| null` | 건조 플레이크 백업 1슬롯 — 말린 시점의 maturity 스냅 |

> (개정 2026-08-24, 저장 v2 — ARCHITECTURE §3) `label`(이름)과 `collection`(도감)은 sim 밖으로
> 이동: 이름은 `StarterRecord.name`(르방 개체 소유), 도감은 `shared.collection`(집의 기록 —
> 르방 폐기·삭제에도 남는다, 항목에 starterId 병기). 이름 규칙(5단계 해금·12자)은 §4·§5 그대로.

**초기 상태** (창조 의식 = 첫 급여 1:1:1):
`mass 180, lastFedAt = createdAt, feedRatio '1:1:1', flour 'white', location 'room', acidity 0, maturity 0, reviveProgress 0`.

저장하지 않는 것: 활성도·부피·기포량·냄새·현재 phase(전부 파생), 해금 비율(stage 파생),
급여 횟수(maturity 중복), 수화율(100% 고정 — 레시피 카드 맛 설명에만 등장), 온도·급여 이력 로그.

## 3. 핵심 수치

### 3-1. 온도 배율 — 모든 수치의 뿌리

| 위치 | 상정 온도 | 배율 | 의미 |
|---|---|---|---|
| 실온 `room` | 24°C | ×1.0 | 기준. 하루 1~2회 리듬 |
| 창가 `window` | 27~28°C | ×1.3 | 서두를 때 — 피크가 이르고 배고픔도 빠름 |
| 냉장 `fridge` | 4°C | ×0.08 | 유지 모드 |

검산: 배고픔 14h ÷ 0.08 ≈ 175h ≈ **7.3일** → "냉장 시 주 1회"가 같은 표에서 저절로 도출.

### 3-1-1. 밀가루 배율 (2026-08-24 신설 — 확장기획 §7-2)

| 가루 | 배율 | 의미 |
|---|---|---|
| 다목적 `white` | ×1.0 | 기준 |
| 통밀 `wholewheat` | ×1.15 | 발효 가속 — 피크가 이르다 |
| 호밀 `rye` | ×1.25 | 최대 가속 + 산미도 같이 빨라짐(산미는 유효시간당 적립이라 자동) |

- **유효시간 배율 = 온도 × 밀가루** (`rateMult`). 알림·예측·부활 게이트 전부 같은 배율.
- flour는 **급여에서만** 바뀐다(effBaseMs=0 리셋) — 균일 배율 회계의 전제.
- ×1.15/1.25는 튜닝 가설(§19-2 "방향 확정·정량 TO VERIFY"). 선택 UI는 성숙 4단계 해금(상세 펼치기).
- 렌더: 통밀 담갈·호밀 회갈 색 톤 시프트(renderParams — 상태 톤 아래 바탕).

### 3-2. 활성 곡선 (급여 후 유효시간, 1:1:1 기준)

```
0h ──── 0.75h ──── 4.5h ══ 6h ──── 12h ──── 14h(배고픔)
 잠복기    상승(smoothstep)  피크 1.0   하강 →0.25   전이
 0→0.15
```

- 잠복기 0~45분: activity 0→0.15 (급여 직후엔 조용 — 실제 현상)
- 피크 4.5~6h: activity 1.0, 부피 최대
- 하강 6~12h: 0.25까지 완만히 → 배고픔 전이

### 3-3. 급여 비율 = 플레이어의 스케줄 도구

| 비율 | 씨앗 | 피크(실온) | 배고픔 | 급여 후 mass | 리듬 |
|---|---|---|---|---|---|
| 1:1:1 (기본) | 60g | 4.5~6h | 14h | 60+60+60 = **180g** | 하루 2회. 아침 밥 → 점심 피크 |
| 1:2:2 (3단계 해금) | 60g | 7~9h | 20h | 60+120+120 = **300g** | 하루 1회. 아침 밥 → 저녁 피크 굽기 |
| 1:5:5 (4단계 해금) | **40g** | 10~12h | 30h | 40+200+200 = **440g** | 밤 밥 → 아침 피크. 제빵사 패턴 |

1:5:5의 씨앗 40g은 실제 고배율 급여 관행(씨앗을 줄인다) 그대로 — mass 상한 480g 안에 들어온다.
새 상태 변수 0개로 깊이 추가: 곡선의 시간축만 비율별로 늘어난다.
**비율 선택이 곧 스케줄 퍼즐** — "피크를 내 일상 어디에 앉힐까"가 이 게임의 조용한 전략층.

### 3-4. 상태 전이

```
활발(active) ──14h──▶ 배고픔(hungry) ──+22h──▶ 시큼(sour) ──+106h──▶ 휴면(dormant) ┬─+48h──▶ (반점) ─+72h▶ (확산) ─+96h▶ 곰팡이(종착)
  ▲                                                                                │    [반점·확산은 휴면의 파생 하위단계]      │
  │◀────────── 급여(활발·배고픔·시큼) ──────────────────────────────────────────────┘                                          │
  │◀── 부활 의식 2급여(휴면·반점·확산 — 1회차가 유효시간 리셋 = 걷어내기) ◀───────────┘                                        │
  │                                                                                                                            │
  │◀── restoreFlake(플레이크 有): maturity×0.6, createdAt 보존(이름·도감은 sim 밖 소유 — 자연 보존), reviveProgress=1 경유 ──┤
  └◀── discardStarter(새 개체): maturity 0, createdAt=now, 이름·도감(전역)·플레이크 보존 ──────────────────────────────────────┘
```

- 곰팡이에서 급여·굽기·이동·말리기 전부 차단(`moldBlocked`) — 종결 2액션(폐기·플레이크 복원)만 통과.
- 죽음은 액션으로는 불가능(씨앗 불가침 유지, §6-2) — 오직 방치 시간만이 죽인다.

시큼·휴면은 **둘 다 배고픔 시점 기준 오프셋**: 시큼 = 배고픔 +22h, 휴면 = 배고픔 +106h
(1:1:1이면 급여 후 14h/36h/120h). 큰 급여일수록 버퍼도 같이 밀린다.

- **배고픔**: 부피 꺼짐, 표면 주름. 급여하면 즉시 활발.
- **시큼**: 배고픔+48h부터 hooch(회갈 액체층) 연출. 급여 2~3회면 산미 회복.
- **휴면**: 기포 0, 색 바램, 숨 거의 정지. 급여해도 즉시 안 살아남 — 부활 의식 필요. **죽음 아님.**

### 3-4-1. 곰팡이 — 유일한 실패

휴면의 파생 하위단계로 반점→확산→사망이 진행된다. 신규 저장 필드는 0개 — `moldStage`·`mold01`·`moldDeadAt`
전부 유효 경과시간의 파생값이다. 재정박(§3-8)이 상대 간격을 보존하므로 시계를 앞뒤로 조작해도 회피할 수
없다 — **결정론**.

| 단계 | 배고픔 기준 오프셋 | 휴면 기준 오프셋 | 1:1:1 절대(급여 후) | 의미 |
|---|---|---|---|---|
| 반점 | +154h | +48h | 168h ≈ 7일 | 예고 1단계. 급여하면 즉시 회복(걷어내기, §3-7) |
| 확산 | +226h | +120h | 240h ≈ 10일 | 예고 2단계. 아직 급여로 살릴 수 있다 |
| 곰팡이(사망) | +322h | +216h | 336h ≈ 14일 | 종착 — 회생 불가. 폐기 또는 플레이크 복원만 가능 |

검산: 창가(×1.3)는 곰팡이가 더 이르다(≈10.8일 — 더위가 곰팡이를 부른다, 다이제틱 경고). **냉장(×0.08)은
사망까지 ≈175일** — 냉장이 실제로 곰팡이를 늦춘다(§3-1). 주말 여행은 무해하고, 1주 방치는 반점(회복 가능)에
그치며, 실온에서 완전히 2주를 방치해야만 사망한다 — 실패가 좁고, 예고되고, 회피 가능하다.

**조기 사망의 수용**: 3단계 미만(생후 8일 이전)은 냉장·플레이크 둘 다 미해금이라 무보험으로 방치되면 그대로
사망한다. 손실이 작고(낮은 maturity, 도감은 보존) 실제 스타터도 첫 주가 가장 취약한 것과 정합한다 —
의도적 수용. (개정 2026-09-05) 사망의 대가 = **그 르방의 성장과 병 속 반죽**. 레시피 해금은 집 최고 단계(§6-2)라 잠기지 않고,
도감·통도 집 것이라 남는다 — 새 반죽을 키워 다시 떼어낼 수 있을 때까지 통에 있는 만큼만 굽는다.

### 3-5. 산미 (acidity 0~100)

누적(유효시간당): 활발 +0.3/h · 배고픔 +1.5/h · 시큼 +2.5/h · 휴면 +0.5/h.
급여 시 희석: ×0.55 (1:1:1) · ×0.40 (1:2:2) · ×0.30 (1:5:5).

검산: 하루 2회 성실 급여 → 평형 4~8(순함). 하루 방치 → ≈37(새콤). 이틀 → 60+(시큼).
한 번 놓친 게 체감되고, 되돌리기는 급여 2~3회로 쉽다.

냄새 구간(관찰 카드 문구): 고소한 밀가루(0~15) / 요거트 새콤(15~35) / 식초 기운(35~60) /
코 찡한 시큼(60~85) / 아세톤 기미(85+, 실제 현상).

### 3-6. maturity 적립

급여가 다음을 모두 만족할 때 +1: ① 직전 급여에서 **유효 6h 이상** 경과(연타 무효) ② 급여 시점 phase가
활발 또는 배고픔(시큼·휴면 중 급여는 회복만 되고 미적립). 하루 자연 상한 2사이클.

### 3-7. 부활 의식 (2세션 의식이 정본)

1. 실온 전제 — 냉장 상태면 "실온에 두고 밥 주세요" 먼저 안내 (실제 관행).
2. 급여 1회 → `reviveProgress = 1`, "12시간쯤 뒤에 다시 밥 주세요".
3. 유효 8h 이후 재급여 → `reviveProgress = 2` → 활발 복귀. 너무 이른 급여는 카운트만 안 될 뿐 무해(코지).
4. **maturity 전부 보존**(이름·도감은 sim 밖 소유라 애초에 영향 없음) — 르방은 기억을 잃지 않는다.
5. **반점·확산 단계(곰팡이 예고 중)의 급여도 같은 1회차로 작동한다** — 급여가 유효시간을 리셋하므로
   그대로 "걷어내고 새 밥"이 된다(§3-4-1). 곰팡이 진행 중에도 회피할 수 있다.

연출(VISUAL.md §4-3): 따라내기→밥은 각 급여에, "지켜보기" 줌인은 2번째 급여에만.

### 3-8. 시계 방어

- **역행**: `now < lastSimulatedAt − 5분`이면 저장된 **모든 타임스탬프**(createdAt·lastFedAt·lastSimulatedAt·
  lastDiscardBakeAt·collection.firstAt·flake.madeAt(있다면), 향후 필드 포함)를 delta만큼 당겨 **상대 간격 보존 재정박**.
  닫힌 함수 모델의 필수 규칙 — 나이 음수·단계 퇴행·크래시 전부 불가. 알림 전량 재예약.
- **미래 점프**: 정직한 catch-up (실제 르방도 시간이 흐르면 그렇게 된다 — 막지 않는다).
- **catch-up 상한 60일**: 실온에서는 곰팡이 종착(≤15일, §3-4-1)과 산미 포화가 고정점이라 그 이상은
  상태 불변 — 안전벨트. 곰팡이 판정 자체는 파생이라 이 캡과 무관하며, 캡은 acidity 적분에만 걸린다.
- **자정·DST**: epoch 차이만 쓰므로 특수 처리 자체가 없음. 로컬 시각은 알림 클램프에만.

## 4. 성장 단계

게이트 = `max(요구 사이클, 요구 일수)` — 둘 다 충족해야 승급.

| 단계 | 이름 | 조건 | 해금 |
|---|---|---|---|
| 0 | 갓 반죽 | 생성 | 온보딩. **D2 가짜 부풀기** 이벤트: 크게 부풀고 "곧 조용해져요. 정상이에요" |
| 1 | 잠잠기 | 3사이클·3일 | 침묵기(부피 표현 ×0.3). "이 시기는 원래 조용해요" |
| 2 | 첫 기포 | 7사이클·5일 | discard 레시피: 팬케이크·크래커 |
| 3 | 어린 르방 | 12사이클·8일 | 플랫브레드·포카치아·스콘, 1:2:2, **냉장 보관 해금** |
| 4 | 성숙 르방 | 21사이클·15일 | 식빵·바게트·깜빠뉴, 1:5:5 |
| 5 | 노포 | 45사이클·30일 | 호밀빵·통밀 깜빠뉴, **병 이름표(label)** |

첫 주 = 튜토리얼 겸 온보딩. 가짜 부풀기·침묵기는 실제 스타터 첫 주 그대로 — "매일 열어볼 이유"를 만들면서 가르친다.

## 5. 플레이어 행동

| 행동 | 입력 | 효과 |
|---|---|---|
| **밥주기** | 메인 버튼 → 중앙 모달에서 비율 선택(해금분만, 기본=최근 사용) + **상세 펼치기**(성숙 4단계 해금 — 밀가루 3종 선택·피크 범위 예보, §3-1-1). **단, 휴면 중 부활 급여는 모달 없이 1:1:1 직행**(개정 2026-08-24: 의식은 담백하게 — implementation-notes M4 참조) | `lastFedAt=now`, 산미 희석, mass 재설정, flour 갱신, maturity 판정. 연출: 덜어내기→가루→젓기 |
| **젓기** | 캔버스 드래그 (현 wobble) | **시뮬 효과 없음** — 슬라임 촉감놀이(코스메틱: 기포 몇 개 + 냄새 문구 노출). 천 덮개 플릭 오프닝 포함(VISUAL §4-5). 배고픔 리셋하면 실시간 계약 위반 |
| **위치 이동** | 실온/창가/냉장 세그먼트 (냉장은 3단계 해금) | `location` 변경 + 화면 톤 연출 |
| **관찰** | HUD 상태 문구 탭 → 관찰 카드 | 부피·냄새·마지막 밥·다음 밥 예상 + **말려두기**(3단계 해금 시 행 노출, `phase==='active' && mass≥80g`일 때만 활성 — §5 "말리기"와 같은 게이트 쌍). **계약(개정 2026-08-25): 정보 + 그 정보에 대한 응답 1개까지, 조작 1개 상한.** 관찰 카드는 "잃을 수도 있다"(곰팡이 예고)를 말하는 유일한 화면이고 말려두기는 그에 대한 유일한 응답이라 이 카드 안에 예외로 편입했다 — **이 한도를 넘는 두 번째 조작을 추가하지 말 것**(액션 서랍화 금지). (개정 2026-08-24: 반죽 탭은 poke와 충돌해 상태 문구 탭으로 이동 — implementation-notes M4 참조) |
| **띄워보기** | **관찰 카드 정보 행**(개정 2026-09-05 — 이전 판: 레시피 화면 버튼 → 09-03 상태 줄 탭). 르방의 상태라 르방 카드에서 말한다 | activity ≥ 0.7 → "동동 떠요. 떼어 두기 좋아요" / 아니면 "아직 가라앉아요". **정보만, 버튼 아님**(조작 1개 상한 유지). 굽기 적기가 아니라 **떼기 적기**다 — 빵은 통에서 나가므로(§6-2) "지금 굽기 좋아요"는 2026-09-05부로 어디에도 없다 |
| **떼어내기** | **우상단 보관 통**(`.pantry-jar`, `? ⚙` 아래) 탭 → `canSplit`이면 확인 모달, **아니면 이유와 남은 시간을 토스트로 답한다**(개정 2026-08-26). 통은 **항상 눌린다** — `disabled`를 쓰면 클릭 이벤트가 안 떠서 "왜 안 되는지"를 말할 기회가 없다(2026-08-25 개정의 실사용 결함: 급여 직후 내내 무반응 → "떼어내기 버튼이 없다"). 잠김 표시는 뚜껑 `.is-locked` 하나. 힌트 문구는 삭제(물건이 놓여 있으면 눌러본다). (개정 2026-08-25: 홈 하단 보조 행에서 이 오브젝트로 승격). **확인 모달 첫 문장 = 지금 떼는 반죽의 상태**(개정 2026-09-05: "한창 부풀어 있어요. 좋은 반죽이에요" / "조금 지쳤어요. 그래도 괜찮아요" / "많이 지쳤어요. 빵이 조금 납작할 수 있어요" — activity 0.7/0.4 밴드) | 씨앗 60g만 남기고 나머지를 보관 통으로. 게이트: 남는 양 ≥30g(`SPLIT_MIN_G`) + 마지막 급여 후 유효 6h 경과(`MATURITY_MIN_GAP_H` 재사용 — 급여가 mass를 리셋하므로 시간 게이트 없인 밥 연타→떼기 연타로 무한 적립된다). `mass = SEED_G`, 통에 **로트** `{g, activity, acidity, flour}` 추가(§6-2 — 뗀 순간의 발효력·산미가 반죽에 따라간다). |
| **빵굽기** | 레시피 카드 → 확인 | §6 |
| **말리기** | **관찰 카드 하단**(3단계 해금 시 행 노출) → 확인 모달. 관찰의 "조작 1개 상한" 슬롯을 채우는 바로 그 하나(개정 2026-08-25: 홈 하단 보조 버튼에서 이동) | 게이트 3단계·활발·mass≥80. `mass −= 20g`, `flake = {madeAt, maturity}` 기록(재실행 시 덮어쓰기) |
| **폐기** | 사망 모달 → "새 반죽으로 시작"(moldy 전용) | 새 개체로 재시작 — maturity 0, createdAt=now. 도감(전역)·플레이크·**이름 보존**(개정 2026-08-24: 이름은 병의 것 — v1의 label 초기화 폐지, 확장기획 §11-2) |
| **플레이크 복원** | 사망 모달 → "말린 조각으로 다시 시작"(moldy && flake 有 전용) | `maturity = floor(flake.maturity × 0.6)`, createdAt·label·도감 보존, 부활 의식(§3-7) 경유. `flake` 소모 |
| **르방 추가** | 홈 칩 줄 + 버튼(항상 노출) → 확인 모달 | 새 르방이 생성(자동 이름 "르방이 N"), 슬롯 상한 3(초과 시 "병이 세 개까지예요"). 이름 짓기는 기존 5단계 게이트 그대로 |
| **획득 연출** (신설 2026-09-03) | 재료 수량 0→1(첫 선물·교환소 구매·광고 배송·치트) 또는 성장 승급 직후 **자동** — 사용자 입력 없음 | **비차단 레이어**(`ui/components/celebrate.ts` — 모달이 아니라 교환소 모달 위에서도 뜬다): 받은 재료 아트가 튀어오르고(오버슈트) 밀가루 모트가 흩어진 뒤 "새로 열린 빵 N종" 스트립이 시차 와이프로 등장. 탭 또는 2.6s 뒤 퇴장. **300ms 안에 온 트리거는 한 장으로 병합**(재료 최대 3 + N, 빵 최대 6 + 외 N종), 떠 있는 동안 또 오면 최소 1.2s 뒤 교체. 승급이면 냉장·비율·이름표 해금도 한 줄씩. "열린 빵" = 해금된 베이스 중 그 재료로 만들 미발견 변형이 있는 것. **다이제틱 규칙**: 밀가루 모트·카드 와이프만 — 하트·별·컨페티·이모지 금지(무캐릭터). 이미 보유한 재료를 더 받는 경우(1→2)는 연출 없이 토스트. `prefers-reduced-motion`이면 페이드만 |
| **설명서 보기** | **우상단 `?`**(⚙ 왼쪽, 신설 2026-08-26) → 중앙 팝업 | **읽기 전용 — 조작 0개.** 6개 문답(르방이란 / 위의 가는 선 / 온도와 속도 / 항아리 / 단계 / 잃을 수 있나) + 홈과 같은 마크업의 축소판 타임라인 + 이름표. **경계**: 개념을 처음 만나는 자리에서 막히지 않게 하는 게 목적이지 수치·규칙을 옮겨 적는 매뉴얼이 아니다. 여기에 버튼을 달지 말 것(관찰 카드의 "조작 1개 상한"과 같은 정신). 근거: 홈 타임라인은 눈금·숫자·이름표 0개라 발효 리듬을 아는 사람에게만 읽힌다 — 실사용 판정 "진입장벽이 너무 높다" |

밀가루·물은 무한 — 병 속 mass(60~480g)는 르방 개체별 유한 자원 그대로다. 다만 **굽기 원가는
2026-08-25부터 mass가 아니라 보관 통**(`shared.pantry`, 집 전역)에서 나간다(§6-2) — 통은
떼어내기로만 채워진다.

## 6. 레시피·굽기

### 6-1. discard 레시피 (판정 없음 — 활용의 기쁨)

게이트: 단계 + **"마지막 급여 후 1회"** 쿨다운(`lastDiscardBakeAt < lastFedAt`일 때만 가능).
그램 원장 없음 — 덜어내기는 밥주기 연출로만 존재.

**v1에서 손대지 않는다**(2026-08-25 보관 통 경제 도입 이후에도 그대로): 팬케이크·크래커·스콘은
여전히 `cost: 0` + 급여당 1회 쿨다운뿐이다. §6-2가 진짜 통(재고)을 들여왔다고 해서 discard까지
그램 원장을 붙이지 않는다 — discard는 경제축이 아니라 처음부터 "활용의 기쁨"이었고, 그 의도는
바뀌지 않았다. 즉흥적으로 확장하지 말 것.

| 레시피 | 해금 |
|---|---|
| 팬케이크 | 2단계 |
| 크래커 | 2단계 |
| 스콘 | 3단계 |

해금 단계는 **집 최고 단계**(§6-2 개정 2026-09-05) 기준이다 — 집이 2단계면 갓 태어난 르방의 덜어낸 반죽으로도
팬케이크를 굽는다(실물로도 어느 스타터의 discard든 팬케이크가 된다). 쿨다운(`lastDiscardBakeAt`)은 여전히 **그 르방**
개체의 것이라 르방이 여럿이면 급여 사이클당 discard 횟수가 그만큼 늘어난다 — 수용(가루 미션 가속은 미미).
discard는 통을 쓰지 않으므로 통 품질 판정(`dough`)의 대상이 아니고 등급도 없다.

### 6-2. 빵 레시피 (보관 통 소모 + 판정) — 2026-08-25 통 경제 개정

| 레시피 | 원가 | 산미 선호 | 엄격도 | 해금 |
|---|---|---|---|---|
| 플랫브레드 | 30g | 0~60 | 관대 | 3단계 |
| 포카치아 | 50g | 0~40 | 관대 | 3단계 |
| 식빵 | 80g | 0~30 | 보통 | 4단계 |
| 바게트 | 60g | 10~35 | 엄격 | 4단계 |
| 깜빠뉴 | 100g | 15~45 | 엄격 | 4단계 |
| 호밀빵 | 80g | **40~75** | 보통 | 5단계 |
| 통밀 깜빠뉴 | 120g | 20~50 | 엄격 | 5단계 |

**원가의 출처가 바뀌었다** — v1은 병 속 mass를 직접 깎았다. 이제 원가는 **보관 통**(`shared.pantry`,
집 전역, §5 "떼어내기"로만 채워진다)에서 나가고, 르방이의 mass는 굽기로 **전혀 변하지 않는다**.

- **표시(개정 2026-08-25)**: 우상단 보관 통(`.pantry-jar`, §5)의 잔량은 숫자가 아니라 **수위**로 읽힌다 —
  정확한 g 값은 `aria-label`에만 남는다. **병 아래 힌트 문구는 삭제**(개정 2026-08-26 — 항아리가
  물건으로 놓여 있으면 눌러본다. "이게 뭐냐"는 탭 응답과 설명서(`?`)가 답한다).
  병은 잠겨 있어도 항상 눌리고, 못 뗄 땐 이유와 남은 시간을 토스트로 말한다(§5 떼어내기).
- **게이트 = 집 최고 단계 + 통 잔량**(개정 2026-09-05). 해금 단계는 **활성 르방이 아니라 집 최고 단계
  `houseStage = economy.stageMax`**로 판정한다 — 통이 집 것이면 해금도 집 것이다(사용자 판정 2026-09-05:
  "모아둔 반죽으로 빵을 굽는데 르방마다 도감이 달라진다"). 어린 르방으로 넘겨도 카드가 잠기지 않는다.
  구현: store가 `doDispatch`에서 bake/bakeDiscard 액션에 `houseStage`를 주입하고 sim은 `max(자기 stage, houseStage)`로
  판정(값 인자 — 순수 유지). **`pantryGate`도 같은 houseStage를 받는다** — 안 받으면 활성 르방 기준 'stage' 차단이라고
  믿고 통 검사를 건너뛰어 원가 0 빵이 나온다(Opus 반론 FM1, 회귀 테스트로 잠금). 통 게이트는 store 소관 —
  sim은 전역 통을 모른다(ARCHITECTURE §2). 통이 부족해도 **빵 시트는 열린다**(개정 2026-09-03 — 원가와 부족량이
  시트 머리에 보이는 게 진입 차단보다 낫다) — `굽기`를 누르면 "보관해둔 르방이 Ng 필요해요" 토스트로 막는다.
  이 빵을 구우면 통이 정확히 0이 되는 조합은 굽기 직전 확인 팝업(중앙 팝업)을 한 번 더
  거친다 — 취소하면 통·mass 둘 다 무차감.
  - 이전 판(2026-08-25~09-04): 해금·판정 모두 활성 르방 stage/activity. 실측(2026-09-05 `390-07`): 통 320g이 있어도
    어린 르방으로 넘기면 10장 전부 "N 단계가 되면 열려요" + 상태 줄 "르방이 2 · 지금 굽기 좋아요".
- **씨앗 불가침이 더 강해졌다**: v1은 `mass ≥ 비용 + 60g`로 씨앗 60g에 여유를 남겨 굽기가 침범하지
  못하게 막았다. 지금은 굽기가 mass를 아예 건드리지 않으므로 그 여유 계산 자체가 필요 없다 —
  **어떤 액션도 르방이를 죽일 수 없다는 물리 보장**(죽음은 오직 오랜 방치(곰팡이) 하나뿐)이 마진이
  아니라 구조로 성립한다.
- **판정 = 통에서 나가는 반죽의 품질**(개정 2026-09-05 — 08-25 "품질 세탁 수용" 결정 **번복**).
  `점수 = 0.6 × activity + 0.4 × 산미적합도`의 activity·acidity·flour를 **활성 르방이 아니라 통에서 이번에
  나가는 반죽**에서 읽는다. 적합도 = 선호 범위 안 1.0, 벗어난 만큼 감점(관대/보통/엄격 = 감점 기울기)은 그대로.
  - **통은 로트 원장이다**: `shared.pantryLots[]` — 떼어낼 때마다 `{g, activity(뗀 순간), acidity(뗀 순간), flour}` 한 로트가
    뒤에 붙는다. 굽기는 **그 빵에 가장 잘 맞는 로트부터**(`sim/pantry.ts pickDough` — 로트별 `bakeScore(recipe, …)` 내림차순,
    동률이면 오래된 것) 원가만큼 골라 쓴다: 시큼한 로트는 호밀빵 때 뽑히고, 지친 로트는 더 좋은 반죽이 남아 있는 동안 나가지
    않는다 — "제빵사가 빵에 맞는 반죽을 고른다". 판정은 고른 조각들의 g 가중 평균, flour 친화는 조각 중 g이 가장 많은 가루.
    로트 상한 12(초과 시 가장 오래된 둘을 g 가중 병합). `shared.pantry`(총 g)는 Σ로트의 미러로 함께 저장한다 — 1.4.x 번들이
    읽는 키라 롤백 안전(RELEASE §8). 타임스탬프 없음 → 재정박(§3-8) 대상 아님.
    - FIFO(오래된 것부터)를 택하지 않은 이유(Opus 반론 1차): 산미 조준이 죽고(호밀은 시큼 로트가 필요한데 앞에 있어야만 나간다),
      피크를 한 번 놓친 사람이 그 로트를 다 구울 때까지 납작한 빵을 강제로 먹는다. best-fit은 둘 다 피한다 — 나쁜 로트는 좋은 게
      다 나간 뒤에야 쓰이고, 그때는 시트 `반죽` 행이 미리 말한다.
    - 단일 스칼라 평균(Σg×activity)도 택하지 않았다: 혼합 풀은 산미를 평균으로 뭉개 레시피별 조준이 불가능하고, 오염된 평균을
      되돌리려면 피크 반죽 수백 g이 든다(회복이 느리다).
  - **떼는 순간이 등급을 정한다**: "피크가 떼기 창"(아래 새 리듬)이 처음으로 이빨을 얻는다. 검산(1:1:1 아침 밥):
    8h 떼기 activity 0.925 → best · 12h 0.33 → good 경계 / 1:2:2 12h → 0.86 best. **flat은 배고픔·시큼 르방에서
    뗄 때만**(≈0.52). 비율 선택(§3-3 "스케줄 퍼즐")이 등급에 연결된다.
  - **통 반죽은 늙지 않는다**(설계): 떼어 둔 반죽은 냉장고에서 잠들어 있다 — 뗀 순간의 상태가 그대로 보존된다.
    피크에 크게 한 번 떼어 두면 그 예치금이 한동안 등급을 받쳐 준다. 시간이 흐르는 건 병 속 르방뿐이다.
  - **레거시(1.4.x 저장본)**: 로트 없이 g만 있으면 읽는 즉시 로트 1개 `{g, activity 0.8, acidity 25, white}`로 물질화한다
    (`PANTRY_LEGACY_*`) — 선호 범위 안 빵은 best(0.88), 호밀빵은 good. 0.75가 아닌 이유: 0.6×0.75 = 0.44999…(부동소수)라
    0.85 경계에서 good으로 떨어진다. 개발자·테스트 주입구(`grantPantry`)도 같은 레거시 품질로 심는다 — 품질 0 그램이
    들어오면 QA가 전부 flat을 본다(Opus 반론 FM2).
  - **화면**: 레시피 상태 줄 = `보관 320g` + 통 전체 평균의 `발효력 좋음 · 순함`(캡션). 빵 시트 정보 행 `반죽` = **이 빵에
    실제로 나갈 반죽**(best-fit 미리보기 `doughFor(recipeId)`)의 밴드, 우세 가루가 다목적이 아니면 `가루 · 호밀` 행(호밀·통밀
    가산점이 숨은 절벽이 되지 않게). 두 값은 다를 수 있다(전체 평균 vs 이 빵에 골라 쓸 로트).
  - **곰팡이의 대가가 바뀐다**(Opus 반론 1차 지적, 수용): 이전엔 르방을 잃으면 새 개체가 0단계라 레시피가 전부 다시 잠겼다.
    집 기준 해금에선 **레시피·도감·통은 남고, 잃는 것은 그 르방의 성장(밥을 다시 키워야 뗄 수 있다)과 병 속 반죽**이다.
    "도감은 집의 기록"(§2 개정 08-24)·플레이크 보험과 같은 결의 결정 — 성장 손실은 개체의 것, 레시피는 집의 것. §3-4-1 참조.
  - 이전 판(2026-08-25~09-04, 폐기): 등급 = 굽는 순간의 **활성 르방** activity/acidity/flour. "통은 재고(양), 등급은 오늘 이
    르방이의 발효력(질)"이라는 프레이밍으로 품질 세탁(시큼한 A에서 뗀 g을 피크의 B로 굽기)을 의도적 이탈로 수용했다.
    사용자 판정(2026-09-05)으로 번복 — 통이 집 것인데 화면의 르방이 등급을 정하는 게 "이상하다". 새 모델에선 세탁 자체가
    성립하지 않는다(반죽이 자기 품질을 들고 간다).
  - 알려진 잔여: 호밀빵 "시큼의 구원"은 activity 가중 0.6 때문에 **이전 판에서도** 시큼 르방(activity ≤0.2)이면 flat이었다 —
    회귀가 아니라 기존 결함. 백로그 "호밀 판정 가중(activity 0.4/산미 0.6) 재조정".
- **밀가루 친화**(2026-08-24, §3-1-1): 호밀빵×호밀 가루·통밀 깜빠뉴×통밀 가루면 적합도 +0.15(클램프 1.0).
  다른 레시피는 flour 무관 — 친화는 `flourAffinity` 데이터가 결정.
- **등급**: ≥0.85 "최고예요" / ≥0.6 "잘 구웠어요" / 그 외 "조금 납작해요. 그래도 맛있어요" — **실패 없음**.
- **호밀빵 = 시큼의 구원**: 방치돼 시어진 르방이 오히려 적기 (실제 호밀 사워도우가 그렇다).
- **커밋은 연출 시작 시** (`flags.pendingBake`에 기록) — 연출 중 강제종료해도 결과 유실 없음,
  재실행 시 미표시 결과 카드 재노출.
- **새 리듬 — 급여가 빌드, 피크가 떼기 창, 통이 굽기 창**: 급여로 mass를 불리고(1:2:2→300g,
  1:5:5→440g), 피크에서 씨앗 60g만 남기고 떼어(§5 "떼어내기") 통에 옮긴다. 게이트가 유효시간(마지막
  급여 후 6h)이라 피크를 정확히 못 맞춰도 나중에 뗄 수 있다 — 단 **통은 뗀 순간의 활성도·산미를 로트로 기억한다**
  (개정 2026-09-05, 이전 판은 "g만 기억"). 늦게 뗀 반죽은 덜 부푼 빵이 된다(실패는 없다 — flat도 "그래도 맛있어요").
  굽기는 통 잔량만 있으면 아무 때나, 어느 르방이 화면에 있든 가능하다.

### 6-3. 레시피 화면 — 빵 · 선반 · 재료 (개정 2026-09-05: 세그먼트 3개, 선반 신설, 정보 행, 타이포 토큰)

(2026-08-24 판 "세그먼트 `레시피 | 도감` + 하위 `빵 | 재료`, 도감-빵 전량 노출(170장), 완성 카드 탭 = 바로 3D"는
**폐지**(2026-09-03). 실측(2026-09-03, 390×844): 도감-빵이 `?` 카드 170장 scrollH 9,012px, 굽기 모달은 옵션 31행 아래에
`굽기`, 교환소는 60버튼 — 사용자 판정 "편의성도 페이지도 난잡함". 2026-09-03 판 `[빵 | 재료]` 2세그는 구운 빵을 볼 자리를
같이 없앴다 — 사용자 판정(2026-09-05) "기껏 만든 레시피가 3D 보기 아니면 못 본다". 실측(2026-09-05): 발견한 변형을 고르면
시트 아트가 "＋ 원" 플레이스홀더(변형 PNG 404 → 베이스 PNG를 건너뛰고 SVG), 시트 메타 ` · ` 체인이 216px 열에서 구 중간에
접힘, 13px 미만 글자 8종, 카드 바닥 들쭉날쭉. 코드 정본: `src/ui/screens/recipes.ts` · `ui/components/breadSheet.ts` ·
`exchangeModal.ts` · `recipeVisuals.ts`(시각 원형) · `main.css` 레시피·선반·정보 행 절)

**구조**: 제목 → **통 상태 줄**(`보관 320g` + 캡션 `발효력 좋음 · 순함` = 통 전체 평균의 밴드, 비면 `보관 없음` + `떼어낸 반죽이
여기 모여요`. 탭 = 통 설명 토스트. **활성 르방의 적기는 여기서 말하지 않는다** — §6-2 개정) → 세그먼트 **한 줄 3개**
`[빵 | 선반 | 재료]`(탭 재탭 = 순환, 힌트 첫 3회 "한 번 더 누르면 선반과 재료로 넘어가요") → 그리드. 탭 루트에 백버튼은
없다(탭바가 복귀 수단, 하드웨어 백 계약은 ARCH §5 그대로).

**빵 그리드(10장)** — 카드는 **고정 슬롯 4단**(아트 / 이름 1줄 / 1행 / 2행)이라 같은 행의 바닥이 맞는다. 문장은 카드가 아니라
시트가 말한다: 1행 = 원가 `르방 100g`(discard는 `덜어낸 반죽`), 2행 = 등급 짧은 판·횟수(`최고 · 3번`) 또는 `보관 부족`(정보 톤).
우상단 **변형 진행 pill `3/16`은 하나라도 만들었을 때만**(`0/26`은 소음). 미해금 = 회색 아트 + 해금 힌트 2행(자물쇠 아이콘 금지).
해금 판정은 **집 최고 단계**(§6-2). 탭 → **빵 시트**(미해금은 힌트 토스트만).

**선반** — 구운 빵만, 2D로. 3D는 선택지로 내려갔다.
- 도감(`shared.collection`)의 항목 전부(베이스+변형)를 **처음 구운 순 최근부터** 큰 타일(아트 88px·이름 2줄·`최고 · 3번`·`8월 26일`)로.
  **`?` 벽은 두지 않는다** — 09-03이 걷어낸 170장을 되살리지 말 것. 비었으면 `아직 구운 빵이 없어요 · 첫 빵은 플랫브레드부터`.
- 타일 탭 → **결과 카드**(중앙 팝업): 아트 152px · 이름 · 정보 행(`최고`·`횟수`·`처음`·`르방`(기록에 starterId가 있고 그 르방이
  살아 있을 때만)) + footer `[3D로 보기] [다시 만들기]`. 3D는 쇼케이스(GLB 없으면 카드로 되돌아옴), 다시 만들기는 그 변형이
  선택된 빵 시트. 모달 위 모달 금지 — 닫고 연다.
- **변형의 얼굴 = 합성 썸네일**: 전용 PNG가 없는 변형(156종)은 **베이스 PNG + 재료 썸네일을 우하단에 놓인 것처럼**(정물 구도,
  원형 바탕·테두리 없음 — 알림 뱃지 문법과 부딪힌다). 전용 PNG(4종)가 있으면 그것. 폴백 사다리 `변형 PNG → 베이스 PNG(+재료) → SVG`
  (recipeVisuals.breadThumb) — 3D 156개를 만들지 않고 156종이 서로 다른 얼굴을 얻는다. 마켓팅 잠금(업데이트-계획 §4 "메인=항아리,
  선반은 레시피 탭 안")과 정합.

**빵 시트**(중앙 팝업의 시트형 = 본문 스크롤·footer 고정, `modal.ts footer` 옵션):
- 머리 = **아트 가운데(92px) · 이름 · 맛 · 정보 행(k/v)**: `필요 · 르방 100g` / `보관 · 320g`(모자라면 값 아래 `100g 모자라요`,
  정보 톤) / `반죽 · 발효력 좋음 · 순함`(이 빵에 실제로 나갈 반죽 — §6-2 FIFO 미리보기) / `가루 · 호밀`(우세 가루가 다목적이 아닐 때만)
  / `최고 · 잘 구웠어요` / `횟수 · 3번` / `변형 · 3/16`. **` · `로 문장을 이어 붙이지 않는다**(VISUAL §7-2 줄바꿈 규율).
- **재료 넣기 = 4열 칩** — `기본` 칩 + **보유 재료**(또렷 + 수량) → 구분선 `아직 없는 재료` → **미보유 재료**(옅게, **이름은 보인다** —
  무엇이 필요한지 여기서 읽힌다, **수량 pill 없음** — 0은 정보가 아니다, 탭하면 교환소 안내). 이 빵에 넣을 수 있는 재료 전부
  (호환 규칙 `rulesForBase`에서 파생). 발견한 변형이 있으면 ✓. 형태가 2개 이상인 재료는 칩 바로 아래 형태 행이 펼쳐진다
  (발견 ✓·`N번 만들었어요` / 미발견 `1개를 넣어요`). 칩 이름 13px 2줄(말줄임 금지).
- 선택 요약 1줄 → footer `[3D로 보기]`(선택된 것이 도감에 있을 때) `[굽기]`. 굽기 규칙·차단 토스트·마지막 통 확인 팝업·변형
  원자 해금·결과 쇼케이스는 §6-2·§6-4 그대로. **상세 화면(push)은 여전히 없다** — 시트 한 겹.
- **변형 도감은 시트 안에 산다**(칩의 ✓와 형태 행), **구운 기록은 선반에 산다**. 미발견 변형에 필요한 재료 이름을 밝히는 근거:
  교환소가 이미 30종을 이름째 보여준다.

**재료(30종) = 4열 칩**(개정 2026-09-05 — 이전 판의 2열 대형 `?` 카드 15행은 교환소의 4열 칩과 이중 문법이었다): 밝혀짐 =
또렷 + 수량 pill(0이면 없음) / 미발견 = `?` 실루엣 칩(이름 숨김, 탭 = 힌트 토스트). "밝혀짐" 판정 = 보유 > 0 이거나 그 재료를
쓴 발견 변형이 하나라도 있으면(변형 키는 `ruleByVariantId`로 해석 — 문자열 포함 판정 금지). 상단 가루 배너(§6-5)는 이 탭에만.
- 아트는 **GLB에서 구운 투명 배경 썸네일**(`/ingredients/thumbs/<id>.png`)이 정본, 없으면 플랫 SVG 폴백(2026-08-26).
- **밝혀진 칩 탭 = 재료 3D 쇼케이스**(실패 시 결과 카드). ★**김(steam)은 뿜지 않는다**(갓 구운 빵의 다이제틱 신호), "다시 만들기"도
  없다. 한 줄 문구 "빵 N종에 넣을 수 있어요"의 N은 **베이스 수**(형태 수만큼 중복 계수하던 버그 2026-09-03 수정).

### 6-4. 재료·변형 레시피 (확장기획 §8·§18)

- **재료는 순수 컬렉팅 축** (사용자 확정): sim에 일절 영향 없음 — 역할은 변형 해금 키 + 도감 수집.
- 재료 **30종** × 형태 **22종**(`IngredientForm` 전체). 수량은 전역(`shared.inventory`), 형태 무관 재료 단위.
  - 2026-08-24 4종: 올리브·초콜릿·딸기·밤
  - 2026-08-25 확장 8종: 호두·크랜베리·무화과·로즈마리·치즈·계피·블루베리·단호박
  - 2026-08-26 확장 18종: 건포도·레몬·바나나·살구·비트·코코넛·피스타치오·귀리·포피시드·
    해바라기씨·아마씨·메이플·팥·고구마·말차·흑마늘·유자·꿀
  - 형태는 2026-08-26에 `flake`(압착 귀리) 하나만 늘었다 — 나머지 17종은 기존 형태를 재사용했다.
  (이전 판은 "12종 × 21종", 그 이전은 "4종 × 13종" — 둘 다 낡은 수치였다.)
- 호환성은 `CompatibilityRule[]` **데이터가 결정** (sim/ingredients.ts — **200행** 카탈로그 =
  2026-08-24 46행(27/13/5/1) + 2026-08-25 확장 43행(18/14/11/0) + 2026-08-26 확장 111행(36/52/23/0)
  합산 = verified 81·conditional 79·experimental 39·blocked 1.
  v1 노출 = verified+conditional = **160행(=변형 160종)**).
  - `https://` sourceRef를 단 행은 **실제로 연 페이지**만이다. 2026-08-26 조사는 발견과 검증을 분리해
    돌렸고 검증이 15건을 강등시켰다 — 등급은 재료가 아니라 **형태**에 매긴다는 §18-1이 그대로 적용돼,
    흑마늘은 원문이 "으깬 페이스트"라 `piece`가 아니라 `crumble`로 내려갔다.
  - ★조사 한계: **웨이백 머신이 이 환경에서 열리지 않아** King Arthur Baking 403 우회로가 없다.
- **변형 굽기 진입점은 빵 시트의 재료 칩이다**(개정 2026-09-03 — 이전 판의 "옵션 리스트 모달"은 31행짜리 리스트가 됐다.
  2026-09-05부터 선반 결과 카드의 `다시 만들기`도 그 변형이 선택된 시트로 들어온다):
  `기본` 칩(§6-2 그대로) + 재료 칩 — 발견한 변형은 ✓·`N번 만들었어요`(무소비 재굽기) / 미발견 + 보유는
  `1개를 넣어요`(첫 발견 = 재료 1 소비) / **미보유는 옅은 칩으로 보이되 고를 수 없다**(탭하면 교환소 안내 —
  시도 전 차단, 소비 0. 보이게 두는 이유: 무엇이 필요한지가 곧 수집 동기). **discard 베이스의 변형(예: 초코칩
  팬케이크)도 같은 시트에서 bakeDiscard 경로**(쿨다운 동일, 판정 없음)를 탄다 — bake 경로만 있는 게 아니다.
- **해금 공식**: 첫 굽기 = 재료 1 차감 + 도감 발견 **원자 커밋**(store.bakeVariant — persist 1회).
  무효 조합·재료 부족은 시도 전 차단(소비 0). 발견한 변형 재굽기는 재료 재소비 없음(mass만).
- 변형 판정·비용·게이트는 전부 베이스 레시피 그대로. 자산도 베이스 GLB 재사용(§8-2 assetId 분리,
  §6-3 "변형 아트는 베이스 재사용"과 동일 원칙).
- 재료 획득 경로는 §6-5(무료 경제).

### 6-5. 무료 경제 (확장기획 §9 — Phase 7, 2026-08-25 신설)

광고·결제 이전에 바닥을 먼저 깐다. **광고는 나중에 "가속"만 판다** — 도달 자체는 무료로 열린다.

- **재화 2종뿐**: 재료 + **교환 가루**. 유료 젬 없음, 만료 없음, 랜덤 없음.
  원하는 재료를 정가로 사는 구조라 도박·불운이 게임에 들어오지 않는다.
- **잔액은 저장하지 않는다** (§1 닫힌 함수 모델): 가루 = 누적 획득 − 누적 사용이고,
  누적 획득은 카운터·도감의 함수(`store/economy.ts`). 이중 지급이 구조적으로 불가능하고,
  타임스탬프가 없어 시계 조작 면역 + §3-8 재정박 목록과 무관하다.
- 저장은 `shared.economy` — feeds·bakes·stageMax·exchanged·spent·gifted. 전부 단조 증가.
  키 부재 = 0(무버전 착륙, flour·inventory와 동일 — OTA 롤백 안전).

| 획득 경로 | 보상 | 비고 |
|---|---|---|
| 첫 재료 선물 | 재료 1개 (30종 중 **선택**) | 1회. 신규는 창조 의식 직후, 기존 저장본은 부팅 시 권함 |
| 누적 급여 10회마다 | 가루 12 | **무한 반복** — 무료 회복이 상존하는 자리 |
| 누적 굽기 5회마다 | 가루 12 | 무한 반복 |
| 성장 단계 최초 도달 | 단계당 가루 12 | **집(계정) 최고 기록** 기준 — 새 르방으로 재수령 불가 |
| 베이스 레시피 최초 완성 | 종류당 가루 12 | 도감에서 파생 (변형 키는 세지 않는다) |

- **교환**: 가루 12 → 원하는 재료 1개 / 재료 1개 → 가루 4. 환율이 3배 차이라 중복 정리는
  이득이지만 사고팔기를 반복해 가루를 불릴 수는 없다.
- **소프트캡 9**: 재료는 종류당 9개까지. 초과 지급분은 자동으로 가루가 된다(버려지지 않는다).
  캡에 도달한 종류는 구매가 거절된다 — 가루는 차감되지 않는다.
- **미션에 실패가 없다**: 기한·연속 기록·리셋이 존재하지 않는다. 누적 횟수만 쌓인다(§10 톤).
- **표면**: 레시피 화면 [재료] 탭 상단의 가루 배너 하나 — [재료 바꾸기](교환소)·[가루 모으기](미션).
  교환소(개정 2026-09-03, 시트형)는 잔액을 **머리**에 두고 재료 30종을 **4열 칩**으로 보여준다(이전 판은 30행 × 버튼 2 =
  60버튼, 잔액이 맨 아래) — 칩을 고르면 footer에 `{재료} · N개 있어요` + [가루 12개로 가져오기] [가루 4개로 바꾸기].
  잠금은 `.is-locked`(탭하면 이유 토스트). 광고 배송 행(§6-6)은 머리 아래. 재료 탭의 ?-실루엣은 "만나 본 기록"이라 축이 다르다.
- **처음 얻는 재료(0→1)는 획득 연출**(§5 표 "획득 연출")이 "새로 열린 빵 N종"까지 보여준다 — 선물·구매·광고 배송 공통.
- **수용 기준(§16)**: 광고 0으로 변형 **160종** 전부 도달 가능(2026-08-26 재료 30종 확장 반영 —
  이전 판은 12종·72종, 그 이전은 40종) — `tests/economy.test.ts`가 실제 급여·떼기·구매·굽기 루프로
  검증한다(선물 1 + 구매 159 = 가루 **1908**). 변형만 노리는 최단 루프는 **급여 1231회**
  (하루 2회 기준 ≈616일)이고, 같은 테스트가 상한 `MAX_FEEDS=1601`(실측×1.3)으로 실측 강제한다.
  - ★**상한을 그냥 올리지 않고 다시 쟀다.** 상한의 정의가 "실측의 약 1.3배"라서, 안 재고 올리면
    그 숫자가 무엇의 1.3배인지 아무도 모르게 된다. 이전 판(72종)의 실측은 521회였다.
  - ★**페이싱 상수는 그대로 두는 게 사용자 확정이다**(2026-08-26). 616일이 길지만 수용 기준은
    "닿는가"이고 닿는다. **가속은 과금이 판다** — 확장기획 §10의 보상형 광고·IAP가 그 경로이고,
    **아직 미구현**이라 현재는 파는 가속이 없다.
  - 병목은 여전히 재료(가루) 쪽이다 — 사이클당 그램 예산이 그 주기에 굽는 양보다 넉넉해 그램은
    병목이 아니다. 핵심 레시피 10종은 재료를 요구하지 않으므로 유료·광고 전용 레시피는 0개다.

### 6-6. 보상형 광고 (확장기획 §10 — 2026-08-30 신설)

**전부 사용자 선택형.** 버튼을 눌러야만 재생된다. 앱 시작·전면 강제 광고, 항아리 화면 광고,
급여 인질, 곰팡이 연출 직후 압박, 되살리기(사망) 광고는 **설계 불변으로 금지**다.

| 슬롯 | 내용 | 하루 상한 | 상태 |
|---|---|---|---|
| 재료 배송(`delivery`) | 광고 1회 = 재료 1개 | 3 | v1 구현 |
| 회복 도움 · 레시피 힌트 · 건조 가속 | §10 설계분 | — | 미구현(각각 sim 액션 신설 필요) |

- 전체 하루 **5회**, 세션 **2회** 상한(`AD_TOTAL_DAILY`·`AD_SESSION_CAP`). "하루"는 기기 로컬 자정 경계.
- 지급 원장(`shared.ads`)에 `{slot, at}`을 남겨 같은 노출의 중복 지급을 0으로 만든다(§10 멱등성).
  무버전 추가 키 — pantry·economy와 같은 등급으로 롤백 안전.
- 지급은 **`grantInto` 재사용** — 소프트캡 9·초과분 자동 환전 규칙을 우회하지 않는다.
  대상은 캡 미만 재료 중 **미보유(0개) 우선** 무작위("신규 보장, 가능한 경우"의 최소 구현).
- 시청 실패·취소·no-fill은 **차감 0**(§10 금지 목록). 상한 판정은 UI와 store 양쪽에서 한다 —
  두 스냅샷이 어긋나 캡을 넘기는 경로를 닫는다.
- SSV(서버 검증)는 백엔드 0이라 미사용 — 클라이언트 보상. 정책 위반이 아니며, 조작 피해가
  조작자 본인에게 귀속되는 오프라인 싱글게임이라 수용한 리스크다(§10-1).
- **SDK 부재 시 슬롯 자체가 없다** — OTA가 광고 UI를 구셸에 실어도 죽은 버튼이 생기지 않는다.
  ★판정은 `isNative() && Capacitor.isPluginAvailable('AdMob')`이다. **`isPluginAvailable` 단독은
  안 된다** — 이 플러그인은 웹 스텁을 등록해서 브라우저에서도 true를 돌려준다(2026-08-30 실측으로
  잡은 결함: 웹 빌드 교환소에 광고 행이 그대로 떴다).

## 7. 알림 (기본 3종 + 옵트인 4종, 하루 상한 2건, 조용시간 설정 가능) — 개정 2026-09-03

`planNotifications(state, now, opts)` 순수 함수 → platform이 고정 id 슬롯으로 cancel→schedule.
트리거: 모든 액션 직후 + **앱 복귀(resume)**(이전 판의 "`App.pause`" 표기는 코드와 달랐다 — 실제 재계획은
`onLifecycle`의 visible 콜백, `app.ts`). 사용자 판정 "푸시가 너무 적다"(2026-09-03)로 슬롯 4→7, 탭 딥링크·설정 표면 신설.

> (개정 2026-08-24, 멀티 르방) 실제 예약은 `planNotificationsAll(sims)` — 알림 총량은 N배가
> 아니라 **슬롯 3개 그대로**. 같은 슬롯을 여러 르방이 원하면 가장 이른 시각 1건(그 르방의
> 문구·반복 채택) + 병합 수(count)로 집계 문구("르방이 2개가 밥을 기다리고 있어요",
> copy.notifyMany). 아래 표·규칙은 르방 1개 기준 그대로 유효하다.

| 슬롯 | 시점 | 문구 |
|---|---|---|
| 1 | 배고픔 진입 예측 시각 | "밥 시간이에요" |
| 1′ | 냉장 중 — 슬롯 1이 **주간 반복 예약**(`every:'week'`)으로 전환 | "일주일이 지났어요. 밥 줄 때가 됐어요" |
| 2 | 휴면 진입 예측 시각 | "르방이 깊이 잠들었어요. 잊지 말고 깨워 주세요" |
| 3 | 곰팡이 예고(`moldWarn`) — 반점이 미래면 반점 시각, 아니면 확산 시각 | "르방이 표면에 반점이 보여요. 아직 늦지 않았어요" |
| 4 | 피크 밴드 시작 시각(`peak`) — 조용시간으로 민 시각이 밴드를 지나면 **스킵**(클램프 금지: "한창"이 거짓말이 된다). **기본 on**(2026-09-03 승격 — 이전 판 기본 off. 기존 저장본에 남은 off는 존중) | "한창 부풀어 있어요. 지금 굽거나 떼어 두기 좋아요" |
| 5 | 시큼 진입 예측 시각 = 배고픔 + 22h(`sour`, 신설 2026-09-03). 밥 D1 뒤 두 번째 손짓 — 사실 + 위로 한 문장 | "조금 시큼해졌어요. 밥을 주면 금방 돌아와요" |
| 6 | 성장 승급 예정 시각(`stageUp`, 신설) — 요구 사이클은 채웠고 **일수만 남았을 때** `createdAt + 요구일`. 사이클이 남았으면 예측 불가라 안 심는다 | "{단계} 단계가 되었어요" |
| 7 | 첫 주 D2 가짜 부풀기 `createdAt + 24h`(`firstWeek`, 신설, 0단계에만). "매일 열어볼 이유"(§4)를 첫날 밖에서도 준다 | "크게 부풀었어요. 곧 조용해질 거예요, 정상이에요" |

슬롯 4~7은 각각 설정 토글(`notifyPeak/notifySour/notifyStage/notifyFirstWeek`, 무버전 추가 키, 기본 on)이고 **활발(실온·창가) 분기에만** 심는다 —
냉장·휴면·부활·곰팡이 분기는 아래 규칙 그대로.

휴면 분기: `dormant && reviveProgress=0` → 슬롯 1 취소(침묵 정합) /
`reviveProgress=1` → 슬롯 1을 유효 8h의 wall 시각(위치 앵커 회계 — 냉장 이동 반영, 2026-08-30 정정)으로 예약, "다시 밥 줄 시간이에요".

**슬롯 3 규칙**: 휴면 후엔 곰팡이 예고 1건(슬롯 3)만 남기고 침묵한다. **사망 시점·사망 후 알림은 0건** —
죽음을 푸시로 통지하지 않는다. fridge·부활 의식 분기를 제외한 모든 분기(활발 포함)에서 예약된다 — 재계획은
액션 시에만 일어나므로, 완전 방치자의 마지막 예약은 마지막 액션 직후에 정해져야 하기 때문이다. 냉장 분기는
슬롯 3을 포함하지 않는다(냉장은 곰팡이까지 ≈175일 + 주간 반복이 이미 재관여를 담당) — 한계 수용: 앱을 영영
안 열면 주간 반복이 사망 후에도 계속 발화할 수 있다(재계획 기회가 없으면 취소 불가).

완전 방치 검산: 총 **4건**(밥 D1·시큼 D2·휴면 D5·곰팡이 D7) 후 침묵(이전 판 3건 + 시큼).

- 조용시간 기본 22~08시, **설정에서 시 단위 변경 가능**(개정 2026-08-30 — 교대근무 대응). 창이 끝나는 시각으로 민다. start=end면 조용시간 없음. 낮 창(start<end)도 허용.
- **하루 최대 2건은 코드가 강제한다**(`capPerDay`, 2026-09-03 — 이전 판은 원칙만 있고 강제 로직이 없었다): 같은 로컬 날짜에
  3건 이상 몰리면 우선순위(곰팡이 > 휴면 > 부활 > 밥 > 시큼 > 피크 > 성장 > 첫 주) 낮은 것부터 드롭. 주간 반복 슬롯은 집계에서 제외.
  멀티 르방 병합 뒤에도 한 번 더 적용.
- Android 채널 2분할(2026-08-30): 케어(밥·냉장·부활·피크·시큼·성장·첫 주, importance DEFAULT) / 중요 알림(반점·깊은 잠, importance HIGH) — 시스템 설정에서 따로 조절 가능.
- inexact 알람(SCHEDULE_EXACT_ALARM 회피). 문구도 "쯤" 뉘앙스로 정합.
- 죄책감 유발 금지. 스팸 금지.
- **알림 탭 딥링크**(2026-09-03): 밥·냉장 주간·부활 2회차·시큼 = 홈 + **밥 모달 자동 오픈** / 피크 = 레시피 탭 / 나머지 = 홈.
  콜드 스타트로 열려 브리핑·곰팡이 모달이 먼저 떠 있으면 그것들이 닫힌 뒤 실행. 온보딩 전엔 무시. 알림 본문 1줄 = 르방 이름(멀티 르방에서 누구 얘긴지).
- **설정 표면**(2026-09-03): 설정 → `알림` 행 → 하위 모달 = 마스터 토글(`알림 받기`) + 항목 4개(한창때·시큼해질 때·단계가 오를 때·첫 주 안내,
  마스터가 꺼져 있으면 `.is-locked`) + 방해 없는 시간 + 권한 안내 행(거부 상태면 "시스템 설정에서 켤 수 있어요").
- 권한 상태기계: 온보딩 말미(첫 밥 후, 가치를 본 뒤) 요청 / 거부해도 전 기능 정상 /
  설정에 "시스템 설정에서 켤 수 있어요"(코어 플러그인엔 앱 설정 딥링크가 없어 안내 문구뿐 — `notifications.ts`).

## 8. 일일 리듬 — 3분 세션

**아침 (2분 30초)**: 열기 → catch-up 스냅(어젯밤 밥의 결과가 이미 반영돼 있음) → 병 탭 관찰
("요거트처럼 새콤한 냄새. 마지막 밥 11시간 전") → 1:2:2 밥(저녁 굽기 계획) → 팬케이크 → 닫기.

**저녁 (3분)**: 열기 → 피크 근처로 한껏 부푼 반죽(개정 2026-08-24: 고무줄 마커 제거 반영) → 띄워보기 "지금 굽기 좋아요" →
깜빠뉴 100g → "잘 구웠어요" → 1:1:1 밥 → 닫기.

**바쁜 주(냉장)**: 일요일 밥+냉장 → 주 1회 알림 → 꺼내서 실온·밥 → 다시 냉장. 주 1회 1분.

### 8-1. 복귀 브리핑

`briefing.ts`의 순수 함수 `deriveBriefing(state, from, to): BriefingKey[]`가 부재(wall-clock) **8h 이상**일 때
"그동안 있었던 일"을 중앙 모달에 시간순 카드로 보여준다. `BriefingKey` 8종: `peaked`(한껏 부풀었다가 가라앉음) ·
`becameHungry`(배가 고파짐) · `hoochAppeared`(표면에 액체) · `becameSour`(시큼해짐) · `wentDormant`(깊은 잠) ·
`moldSpotted`(반점) · `moldSpread`(반점 번짐) · `moldDied`(곰팡이). 부활 의식 1회차 대기 중(`reviveProgress=1`)이거나
시계가 역행한 직후에는 빈 배열 — 의식과 무관한 오보를 막는다.

### 8-2. 진단 순간

관찰 카드(§5 관찰 계약 — "정보 + 응답 1개까지" — 유지)에 새 신호 3종이 더해진다: **kahm**(창가 위치 + 시큼
구간에서 파생 — 주름진 흰 막, 무해), **잿빛**(휴면 중 겉모습만 그렇다는 안내), **아세톤 기미**(냄새 구간
85+, §3-5). 이 3종은 정보만 제공하고 대응은 카드의 응답 슬롯(말려두기)이 아니라 기존 액션에 매핑된다 —
걷어내고 밥 = 홈 밥주기 버튼, 폐기·플레이크 복원 = 사망 모달 전용. 조작 1개 상한을 넘기지 않는다.

## 9. 온보딩 (첫 실행)

저장 없음 → **창조 의식**: 밀가루+물 섞기(드래그로 젓기 — 기존 wobble 인터랙션이 첫 터치포인트) →
`createdAt` 기록, 초기 상태 생성 → 0단계 스크립트 주간이 튜토리얼을 겸한다 →
첫 밥 직후 알림 권한 요청. 완료 플래그는 `flags.onboarded`.

의식 직후 **첫 재료 선물**(§6-5) — 30종 중 하나를 고른다. 고르면 변형 하나를 바로 열 수 있다.
기존 저장본(온보딩을 이미 지난 사용자)은 부팅 브리핑 뒤에 같은 모달을 권하고, 놓쳐도
재료 탭 배너에서 언제든 받는다 (`economy.gifted`).

## 10. 문구 원칙

- 전 문구 `src/ui/copy.ts` 한 파일 — 전수 감수 가능.
- 담백한 한 문장. 시스템어·내부 용어(활성도, phase, catch-up, 스키마) 절대 노출 금지.
- 죄책감 유발 금지, 느낌표·의성어 남발 금지.
- 죽음 문구 = 사실 서술 + 위로 한 문장. 원인 추궁·"~했더라면" 화법 금지.
- 예: 저장 실패 → "기록을 못 남겼어요. 다시 해볼게요" / 배고픔 → "밥 먹을 시간이에요" /
  휴면 → "깊이 잠들어 있어요" / 부활 성공 → "다시 숨쉬기 시작했어요" /
  방치 복귀 → "괜찮아요. 르방은 잘 버텨요."

## 11. v2 백로그 (v1에서 의도적으로 컷)

시간대 틴트 · 물 리본 메시 · 어댑티브 fps 가드 · 사운드 잔여(물 등) ·
피크 옵트인 알림 · 무응답 단계별 문구 · discard 그램 경제 · 굽기 3D 시퀀스(v1은 카드 리절트) ·
부활 급여의 비율 선택(v1은 1:1:1 직행) · iOS ·
계보 UI · 냄새 도감 · 이달의 빵 · 분홍 Serratia · 49°C 과열 · 플레이크 다회 슬롯 · 사망 추모 연출 · float test 재해석.

천 덮개 연출·젓기·사락 사운드는 v1로 승격됨(VISUAL §4-5·§6).
