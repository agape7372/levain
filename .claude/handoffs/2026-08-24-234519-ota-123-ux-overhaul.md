# Handoff: OTA 1.2.0→1.2.3 발행 — Phase 4·6 완주 + 실기기 피드백 UX 개편

## Session Metadata
- Created: 2026-08-24 23:45
- Project: C:\Users\agape\Desktop\코딩\levain
- Branch: **main** (origin/main과 동기 — 이 세션에서 4회 푸시)
- Session type: 구현 마라톤 (스와이프 → Phase 4 → Phase 6 → 실기기 피드백 루프 1.2.1~1.2.3)

### Recent Commits (이 세션 전부)
- 3e3c318 chore: OTA 1.2.3 발행 (레시피/도감 UX 개편)
- (직전) feat: 레시피/도감 UX 개편 — 상세 화면 폐지·바로 3D·굽기 모달 재료 추가·?-실루엣 도감·르방 추가 버튼
- a86267d fix: 빵 도감 필터 칩 간격 + OTA 1.2.2
- 89e9faa chore: OTA 1.2.1 (개발자 모드 + 세그먼트 간격)
- 01f3a96 chore: OTA 1.2.0 발행
- 8c80f70 feat: 재료함·변형 레시피·빵 도감 — Phase 6 (§8·§18)
- 5e96baf feat: 밀가루 축 + 밥주기 상세 펼치기 — Phase 4 (§7-1·§7-2)
- f2f22c1 feat: 배경 스와이프 르방 전환 (§5-5) + Levain Lab 설치 레이스 수정

## Handoff Chain
- **Continues from**: [2026-08-24-200500-ota-110-released-merged.md](./2026-08-24-200500-ota-110-released-merged.md)
  - 그 핸드오프의 "다음 단계"(스와이프 → Phase 4 → Phase 6)는 **전부 완료·발행됨**.
- 정본 기획서: `르방이-확장기획-2026-08-24.md` — 유효하나 §8-1/§8-3의 UI 서술은
  이 세션의 사용자 개편으로 **GDD §6-3·§6-4가 우선** (기획서 원문과 충돌 시 GDD).

## Current State Summary

**워킹트리 클린 · main = origin/main · 테스트 140/140 · OTA 1.2.3 라이브(sha256 재해시 검증).**

이 세션에서 끝난 것:

1. **스와이프 전환**(§5-5) — 배경 시작·수평 우세·up에서 누적 48px 확정. SceneHost 필드 위임.
2. **Phase 4** — flour 축(`rateMult = 온도×가루`, 호밀 ×1.25·통밀 ×1.15 — 튜닝 가설),
   비율 모달 "자세히"(4단계 해금: 가루 선택+피크 범위 예보), flourAffinity 판정 가산, 색 톤.
3. **Phase 6** — 카탈로그 46행(`sim/ingredients.ts`), shared.inventory, store.bakeVariant
   원자 해금, 변형 40종.
4. **개발자 모드**(1.2.1) — 설정 하단 "르방이 vX.Y.Z" **7탭** → 만렙/재료+9/도감 완성.
   `src/version.ts` APP_VERSION = 발행 절차 0단계(수동 갱신).
5. **레시피/도감 UX 개편**(1.2.3, 사용자 확정) — 아래 Important Context 1.
6. **르방 추가 정식 UI** — 홈 칩 줄 +. DEC-5 중 "추가 UI" 소진(개명 자유화만 유보).

## Important Context (다음 에이전트 필독)

### 1. 현행 레시피/도감 UX (기획서 §8 원문보다 우선 — GDD §6-3·§6-4 정본)
- 상세 화면(breadDetail.ts) **삭제됨**. 완성 빵 탭 = 바로 3D 쇼케이스, 하단 "다시 만들기"
  (감상 진입에만 — 굽기 결과 연출엔 없음).
- 변형 굽기 = **굽기 모달에서 재료 추가**: 기본 + 발견변형(무소비) + 미발견·보유("{재료} 1개를
  넣어요"). 미발견·재고0은 미노출. discard 베이스 변형은 **bakeDiscard 경로**(이 세션에서
  버그 발견·수정 — bake는 bread 전용이라 차단됐었다. variantId 패스스루는 bake·bakeDiscard 양쪽).
- 세그 [레시피|도감] + 도감 하위 [빵|재료] — 두 줄 **경계 없이 이어붙임**(`seg--joined-top/bottom`).
  사용자가 간격에 민감하다: 세그↔하위줄 = 0, 줄↔그리드 = 14px. 바꾸지 말 것.
- 도감 50항목 전량: 발견 = 아트+이름만(**메타 금지** — 사용자 확정), 미발견 = ?-실루엣
  (`.recipe-card.mystery` + `.mystery-mark`). 재료 밝혀짐 = 보유>0 ∨ 발견 변형 사용(파생).

### 2. OTA 발행 절차 (이 세션에서 4회 반복 검증)
```
src/version.ts APP_VERSION 갱신 → npm run ota:release -- <ver>
→ cd ota && npx vercel --prod --scope jirings-projects
→ manifest 캐시버스터로 확인(?t=) + zip 다운로드 sha256 재해시 대조
```
- curl로 manifest가 옛 버전으로 보이면 엣지 캐시 — 캐시버스터 붙이면 됨(앱은 원래 붙여 읽음).
- 적용 트리거 = 백그라운드 전환 후 **복귀** (2회: 1회차 다운로드·예약, 2회차 적용).
- **롤백**: 1.1.0까지 안전(flour·inventory·retapHints·retapHint 전부 v2 무버전 관대 키 —
  옛 코드가 조용히 버림). **1.0.x 금지**(v2 자체를 못 읽어 저장 소실).

### 3. 카탈로그 재구성 편차 (감사 대비)
§18 조사 원문 유실 → 기획서 하이라이트에서 재구성: **27V/13C/5E/1B = 46행**
(기획서 요약 24/16/5/1과 편차 — 명시 목록 우선 원칙). tests/variants.test.ts가 체크섬 회귀 감시.
sourceRef는 기획서 절 번호만(URL 재조작 금지).

### 4. 사용자 실기기 피드백 루프 패턴 (이 세션의 작업 방식)
사용자가 폰 스크린샷으로 피드백 → 소수정 → 버전 올려 즉시 OTA. 1.2.1~1.2.3이 이 루프.
motionlab(LAN http://192.168.0.6:5199/motionlab.html)도 병행 사용 중.
"물리 반영 안 됐다" 류 보고는 대개 **상태 의존 물성**(피크만 탱탱, 갓밥준 = 묽음, 배고픔 = 뻣뻣)
오해 — 실측으로 답할 것(갓 반죽 liquidity 0.82 확인 이력).

### 5. 위임 정책 준수 확인
이 세션 Sonnet 위임: 변형 프롬프트 JSON 10종·ingredientArt.ts·GDD/QA 문서 개정 — 전량 검수 통과.
단순작업은 계속 위임할 것(사용자 재강조).

## Pending Work
- [ ] **Phase 7 — 무료 경제** (다음 최우선): 미션·교환 가루·소프트캡 9·온보딩 재료 1개 지급.
      **이게 나가야 변형 40종이 실사용자에게 열린다** — 지금은 개발자 모드·Lab 전용.
- [ ] Phase 8~9 — 광고·IAP (도입 여부 자체가 사용자 미확정).
- [ ] DEC-5 잔여 — 개명 자유화(5단계 게이트 해제 + 각인/칭호 전환). 추가 UI는 소진됨.
- [ ] 변형 전용 GLB 승격 — 프롬프트 JSON 10종 준비됨(`assets/prompts/breads/*--*.json`,
      그록 일괄 투척용). 사용자가 이미지 뽑아오면 img2threejs 파이프라인(BREADS.md).
- [ ] `uSlopeAspect` 실기기 하향 튜닝 / VISUAL §1-2 눈금 재도입 (백로그 유지).
- [ ] APP_VERSION 자동 주입(ota-release.mjs에서) — 현재 수동 2계좌 갱신, 드리프트 위험.

## Immediate Next Steps
1. 사용자 폰 1.2.3 확인 대기(설정 하단 버전 표기) — 추가 UI 피드백 오면 같은 루프.
2. 구조 작업 재개 시 = **Phase 7**(기획서 §9 정본). 수용 기준: "광고 0으로 전 변형 도달 가능".
3. Phase 7 후 다음 OTA에서 온보딩 재료 지급 포함 여부 사용자 확인.

### Blockers/Open Questions
- [ ] 광고·IAP 도입 여부 (PRIVACY "수집 없음" 선언 반전이라 별도 확정 필요).
- [ ] 스토어 카피 "사워도우" 사용 여부 (잠금 문서 자기모순 — 미해결 유지).

## Critical Files
| File | Purpose |
|---|---|
| `src/ui/screens/recipes.ts` | 세그·굽기 모달·도감(?-실루엣) — 이번 개편의 중심 |
| `src/sim/ingredients.ts` | 카탈로그 46행 정본 + variantIdOf/rulesForBase |
| `src/store/gameStore.ts` | bakeVariant 원자 해금·dev 치트·addStarter |
| `src/ui/screens/ingredientArt.ts` | 재료 SVG 4종 (Sonnet 산출) |
| `src/version.ts` | APP_VERSION — OTA 발행 0단계 수동 갱신 |
| `docs/GDD.md` §6-3·§6-4 | 현행 UX 정본 (기획서 §8 원문보다 우선) |
| `scripts/levainlab.ts` | 픽스처·재료 지급·시간 이동 (설치는 pending 키 경유 — 레이스 주의) |

## Key Patterns Discovered
- **Levain Lab 설치 레이스**: install이 저장에 직접 쓰면 reload의 visibilitychange saveNow()가
  덮는다 — pending 키 경유(f2f22c1에서 수정, 재발 금지).
- 설정 모달 숨은 진입 = 버전 텍스트 7탭 — dev 치트 게이트 패턴.
- Playwright로 levainlab 구동 검증이 표준 루프(브라우저 페인 숨김 시 Claude_Browser 대신
  playwright MCP 사용 — 렌더 없이도 evaluate 가능).
- 텍스트 셀렉터 함정: '만렙 (…재료량…)'과 '재료 전부 +9' 둘 다 '재료' 포함 — 정확 일치로 찾을 것.

## Environment State
- dev 서버(levain-dev, 5199, --host) 세션에 살아 있음 — 종료 시 정리 무방.
- OTA: manifest 1.2.3, bundles/ 로컬+Vercel에만(4개 초과분 자동 정리 — 1.1.0 zip 잔존 확인됨).
- 위키 `06_wiki/levain-status.md`·메모리 `levain-expansion-track.md` 이 세션 내용까지 갱신 완료.
- implementation-notes.md(.gitignore, 이 PC 전용)에 세션 전체 결정 이력 — 상세 정본.
