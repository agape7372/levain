# 르방이 확장 기획서 — 촉감·멀티 르방·재료·경제

기준일 2026-08-24 · 대상 레포 `C:\Users\agape\Desktop\코딩\levain` (브랜치 `grok/LV-001`, 미커밋 67항목)
작업 모드: **감사·기획 전용**. 레포 무수정. 감사 4건(sim·저장/플랫폼·렌더/입력·UI/문서) +
조사 4건(사워도우 현실·레시피 카탈로그·광고/결제 정책·점탄성 기법) 전량 반영 완료.

---

## 1. Context — 왜 이 기획인가

`levain`은 이미 출시 직전 제품이다. GDD·ARCHITECTURE·VISUAL 3개 정본이 상태머신·수치·uniform까지 잠갔고,
빵 GLB 10종과 절차 파이프라인까지 끝났다. 사용자가 새로 던진 요구는 네 덩어리다.

1. **촉감** — 인스타 릴스의 르방처럼 쫙쫙 늘어나야 하는데 지금은 고무줄처럼 튕긴다.
2. **멀티 르방** — 좌우 스와이프 전환 + 이름.
3. **밥주기 현실화 + 유휴 모션 다양화**.
4. **재료·변형 레시피 + 광고·결제 경제**, 그리고 개발자가 모든 상태를 즉시 보는 도구.

이 중 3·4는 이미 잠긴 정본과 정면으로 부딪친다. 그래서 이 기획의 본체는 "기능 추가"가 아니라
**정본 충돌 해소 + 되돌리기 비용이 큰 결정 4개의 확정**이다.

---

## 2. 보존해야 하는 것 (건드리면 제품이 아니게 되는 선)

- 실시간 wall-clock 다마고치. rAF는 렌더 전용.
- `src/sim/` 순수성 — three/DOM/Capacitor/`Date.now()` import 0.
- 닫힌 함수 모델 — 파생 가능한 값 저장 금지.
- 무캐릭터 — 눈·표정·하트·별 금지. 연출은 전부 다이제틱.
- 담백한 한국어 · 죄책감/빨강 경고 0 · 모달은 중앙 팝업.
- 씨앗 60g 불가침 — **어떤 액션도 르방이를 죽일 수 없다**. 죽음은 방치뿐.
- 저사양 Android: draw call ≤10, 단일 WebGL 컨텍스트, DPR ≤2.

---

## 3. 현재 구현 감사표 — 요구 A~E 대비

범례: ✅ 이미 있음 · 🟡 부분 구현 · ❌ 없음 · ⚠️ 문서·구현 충돌

### 3-1. 사용자 요구 A (르방 본체)

| 요구 | 판정 | 코드 근거 |
|---|---|---|
| A1 쫙쫙 늘어나는 촉감 | ❌ | 현 stir는 손가락 **속도** 기반 시어장(`uStirPos`/`uStirVec`) + `uTrail[4]` — 잡은 점의 **변위**가 아니다 (VISUAL §3-2·§5) |
| A2 고무줄 텐션 과함 | 🟡 원인 특정됨 | 복귀가 damped spring `e^(−5t)·cos(12t)` 고정 (VISUAL §5 이징 3종) — 점성 성분 0 |
| A3 기포 순화 | 🟡 | 8슬롯 양수 돔으로 이미 1차 순화(v1.1에서 음수 함몰 4개 폐기). 다만 팝 시 `−0.3max` 함몰 잔존 (VISUAL §2) |
| A4 좌우 스와이프 르방 전환 | ❌ | 개체가 1개뿐. `input.ts` FSM은 poke/롱프레스/stir 3분기만 |
| A5 르방 이름 | 🟡 **충돌** | `label`은 **5단계(45사이클·30일) 보상**이고 사용자가 직접 입력 (`settingsModal.ts:66-88`, `actions.ts:147-152`). "처음부터 이름" 아님 |
| A6 기본 이름 `르방이 N` | ❌ | 자동 이름 생성 코드 0. `label`이 null이면 홈 칩에 단계명만 |
| A7 밥주기 세분화 | 🟡 | 비율 3종 **이미 있음**(1:1:1 / 1:2:2 / 1:5:5, 해금 stage 0/3/4). 밀가루 종류·수화율·그램 입력은 ❌ |
| A8 상태별 유휴 모션 다양화 | 🟡 | 상태→uniform 앵커 표 5행 존재(VISUAL §3-3)이나 모션 자체는 **호흡 사인파 + FBM + 기포** 3요소뿐. 변주 없음 |

### 3-2. 요구 B~E

| 요구 | 판정 | 근거 |
|---|---|---|
| B1~2 개발자 전체 해금·상태 프로토 | ❌ | 계정 없음. dev 계기판은 `window.__levainScene` 하나(씬 전용) |
| B3 "내 계정만 해금" | ⚠️ 구현 불가 | 백엔드 0·계정 0. 앱 전체 fetch **1곳**(OTA 매니페스트 GET)뿐 |
| B4 레시피 탭 재탭 → 재료관리 | ❌ | 재탭 동작 없음. 탭 2개(르방/레시피) |
| C 재료·변형 레시피 | ❌ | `RecipeDef`에 재료 개념 0. 10종 고정 배열 (`recipes.ts:11-25`) |
| D 광고 | ❌ + ⚠️ | SDK 0. `docs/PRIVACY.md`가 "광고·분석·제3자 SDK 미사용" **공개 선언** |
| E 결제 | ❌ + ⚠️ | Billing 0. `docs/STORE_LISTING.md`가 "광고 없음 · 인앱 결제 없음 · 데이터 수집 없음 · 완전 오프라인" 선언 |

### 3-3. 이미 구현돼 있어 **다시 만들면 안 되는 것**

사용자 아이디어와 겹치지만 이미 산 코드 — 재설계 대상이 아니라 **확장 대상**이다.

| 항목 | 현황 |
|---|---|
| 밥 비율 3종 + 스케줄 퍼즐 | `RATIOS` (`constants.ts:30-32`), 비율별 시간축 전체가 이월됨 |
| 냉장 보관 | `location:'fridge'` ×0.08, 3단계 해금, 주간 반복 알림 |
| 건조 백업 | `flake` 1슬롯 — `makeFlake`(mass −20g) / `restoreFlake`(maturity ×0.6) |
| 후치 | `HOOCH_AFTER_HUNGRY_H=34` 파생 + 렌더 `uHoochAmt` + 브리핑 키 |
| kahm(무해 흰 막) | `derive.ts:153` — 창가+시큼 구간 파생. **곰팡이와 이미 구분돼 있다** |
| 곰팡이 예고 2단계 | 반점(+154h) → 확산(+226h) → 사망(+322h), 전부 결정론 파생 |
| 도감 | `collection: Record<recipeId, {bestGrade, count, firstAt}>` |
| 복귀 브리핑 | `deriveBriefing` 8키 |
| 빵 3D 10종 | GLB 1090KB/2560KB + 썸네일, `enterShowcase(url)` 단일 컨텍스트 재사용 |

### 3-4. 문서 드리프트 (기획 진행 전 확정 필요)

| # | 충돌 | 근거 |
|---|---|---|
| D1 | 마켓팅 잠금 문서는 스토어 카피에서 "키우기·타마고치·레시피" 금지 ↔ `docs/STORE_LISTING.md`는 "키우기 게임"·레시피 나열 | `르방이-업데이트-계획.md` vs `STORE_LISTING.md` |
| D2 | VISUAL §1-2 "고무줄 마커 = 부피 정보의 정본 표시 장치" ↔ 실제로는 **사용자 확정으로 제거됨** | `implementation-notes.md` 2026-08-23 저녁 |
| D3 | GDD §5 "병 탭 = 관찰" ↔ 실제는 상태 문구(HUD) 탭 | `implementation-notes.md` M4 |
| D4 | GDD §5 부활 급여 비율 모달 ↔ 실제 1:1:1 직행 | 〃 |
| D5 | VISUAL §4-2 굽기 5.5s 3D 시퀀스 ↔ 실제 카드 리절트 | 〃 |
| D6 | `actions.ts:148` label 게이트가 리터럴 `5` — CLAUDE.md 규칙 9(수치는 constants.ts) 위반 여지 | `actions.ts:148` |
| D7 | `setLabel` 경로 **테스트 0건** — 이름 기능을 확장하기 전에 잠가야 함 | tests/ 전체 |

### 3-5. 확장 전 반드시 알아야 할 기술 지뢰 4개

1. **마이그레이션 순서 폭탄** — `parseEnvelope`가 `validateAndClamp` → `migrate` 순서다(`persistence.ts:243-244`). v2를 이 순서 그대로 추가하면 **v1 저장본이 v2 가드에 걸려 null → 조용히 새 게임**. 코드 주석(`persistence.ts:167-171`)이 이미 경고 중. **멀티 르방의 첫 번째 작업은 이 순서를 뒤집는 것.**
2. **새 타임스탬프 = 재정박 목록 동시 갱신** — `advance.ts:16-31` `reanchor`가 7종을 당긴다. 르방마다 타임스탬프가 늘어나므로 **N개 전부**를 돌아야 한다. `tests/clock.test.ts:22-47`이 개별 assert.
3. **`getEnvelope()`에 방어 복사 없음** (`gameStore.ts:126`) — 멀티 르방 배열을 UI가 직접 만지면 단일 진실 소스가 조용히 깨진다.
4. **OTA로는 네이티브 플러그인을 못 넣는다** — 번들은 `dist/`뿐. `capacitor.build.gradle`의 모듈 8개는 컴파일 타임 고정. **광고·결제는 무조건 새 AAB.** 게다가 `resetWhenUpdate: true`라 APK가 올라가면 받아둔 웹 번들은 버려진다.

### 3-6. 기획과 무관하게 **지금 잡아야 하는 확인된 버그 2건**

두 건 다 본세션이 코드로 직접 확인했다. 멀티 르방·재료함이 화면을 더 늘리기 전에 고쳐야 한다 —
화면이 늘수록 재현 경로가 늘어난다.

**BUG-1 · 쇼케이스 중 탭 전환 → 레시피 탭 영구 백지 (심각)**

- `Router.push()`가 이전 화면에 `el.style.display='none'`을 건다 (`src/ui/router.ts:41`).
- `Router.back()`은 `top.el.style.display=''`로 복구한다 (`router.ts:53`).
- **`Router.setRoot()`은 복구하지 않는다** (`router.ts:29-37`) — `el.remove()` 후 그대로 다시 append.
- `home`/`recipes` Screen 객체는 앱 수명 동안 1회만 생성돼 재사용된다 (`app.ts:141-148`).
- 재현: 레시피 탭 → 굽기 → 쇼케이스 push → **탭바 아무거나 1탭** → 레시피 탭이 `display:none`인 채 루트로 복귀.
  이후 `location.reload()` 전까지 레시피 탭 백지. 카드가 안 보이니 자가 복구 경로도 없다.

**BUG-2 · 탭바가 모달 위에 그려지고 조작 가능 (BUG-1의 트리거)**

- `#ui-root`가 `position:absolute; z-index:10`으로 **스태킹 컨텍스트**를 만든다 (`main.css:59-64`).
- 그 안의 백드롭 `z-index:40`(`main.css:226`)·토스트 `z-index:50`(`main.css:288`)은 컨텍스트에 갇힌다.
- body 직속 `#tabs`는 `z-index:20`(`main.css:75`) — **컨텍스트 밖이라 위에 그려진다.**
- 결과: 확인 모달이 떠 있어도 탭바가 딤 처리되지 않고 눌린다.

---

## 4. 촉감 감사 — "고무줄" 판정의 정확한 원인 (코드 확정)

렌더 감사로 원인이 **4중으로 특정**됐다. 추측 아님, 전부 file:line 확정.

| # | 원인 | 근거 |
|---|---|---|
| M1 | **grab anchor가 없다.** 변형 소스가 포인터 "속도"뿐 — `vx=(ox-lastHitX)/dt`(`input.ts:93-94`) → `uStirVec`(±0.22 clamp, `DoughMesh.ts:115-118`) → `p.xz += uStirVec*sfall`(`dough.vert.glsl:37`). **잡고 멈추면 변형이 τ≈83ms로 소멸** — "당긴 상태 유지"가 물리적으로 불가능 | 렌더 감사 §결론 |
| M2 | **놓기 판정이 pointerup이 아니라 "80ms 입력 정지"** (`STIR_IDLE_S=0.08`, `DoughMesh.ts:17,145`). 손을 대고 있어도 80ms 멈추면 스프링 발동 | 〃 |
| M3 | **복귀 스프링 ζ≈0.417 → 24% 오버슈트, 주기 0.58s** (`a=-144x-10v`, `DoughMesh.ts:163-164`). 소성(plasticity) 0 — 영구 변형 담는 상태 변수 없음, 완전 탄성계 | 〃 |
| M4 | **wobble이 손가락 절대좌표 비례 전신 오프셋** (`input.ts:98-101`) — 놓으면 τ≈0.167s로 스냅백. 두 번째 튕김의 원인 | 〃 |

부수 발견: 레이캐스트 평면 y=0.55 고정(`input.ts:36`)인데 반죽 윗면은 fill 1.0에서 y≈0.984 —
**손끝과 눌리는 지점이 어긋난다**(≈0.257 오브젝트 단위). fill 연동 필요.

### 4-1. 목표 촉감 (마켓팅 잠금 문서와 정합)

- 잡은 지점이 손가락 **변위**를 따라온다 (grab).
- 당길수록 목이 가늘어지고(necking) 얇아진 부분이 밝아진다(windowpane 근사).
- 놓으면 **즉시 복귀하지 않는다** — 일부 변형이 0.8~1.4s 잔류했다가 천천히 처짐. 오버슈트 ≤1회, 낮게.
- 기포: 팝 함몰(`-0.3·maxAmp`) 완화, 저대비 밝은 돔 위주, 동시 가시 2~4개.
- 상태별 물성 분리: just-fed(질고 무름)/peak(탱탱)/hungry(처짐)/dormant(뻣뻣, 모션 거의 0).

### 4-2. 접근법 3안 비교 → **A안 추천**

| 안 | 내용 | 촉감 상한 | 비용 | 리스크 |
|---|---|---|---|---|
| **A. 셰이더 2시간척도 점탄성 (추천)** | 기존 uniform 파이프라인에 grab anchor + 빠른 탄성 + 느린 creep 성분 추가. CPU는 스칼라 몇 개 적분, 변형은 여전히 버텍스 셰이더 | 목표 장면 충족 | 낮음 — 기존 구조 유지, draw call 불변 | 셰이더 커널 튜닝 반복 필요 |
| B. spring-mass/Verlet 격자 (CPU 12×12) | CPU 물리 → 정점 반영 | 더 높음(전파·출렁임) | 61,440 비인덱스 정점에 매핑 비용 + 저사양 CPU 부담 | 주사율 독립성·안정성 새 문제 |
| C. PBD/soft-body 라이브러리 | 본격 물리 | 최고 | 번들·성능·통합 전부 비쌈 | 과잉. 저사양 가드 위협 |

A안 골자 (개념 — 구현 코드 아님):

1. **입력을 2채널로 분리**: 기존 stir(속도, 젓기 유지) + 신규 **grab**(포인터다운 시 anchor 기록,
   `grabDisp = 현재점 − anchor`를 새 uniform으로). FSM 분기: 반죽 위 down 후 천천히 끌면 grab,
   빠르게 문지르면 stir.
2. **2시간척도 응답**: 변형 상태를 CPU 스칼라 2개로 — `elastic`(τ≈0.1s 추종) + `creep`(당기는 동안
   0.7s+부터 서서히 grab 방향으로 누적, 놓아도 즉시 안 빠짐, τ≈1.2s로 해소). 셰이더는
   `uGrabDisp = elastic + creep` 하나만 읽음. 표준 선형 고체(SLS) 근사.
3. **복귀 재튜닝**: 놓을 때 elastic만 임계감쇠 근처(ζ≈0.9~1.0, 오버슈트 ≤5%)로 복귀,
   creep은 오버슈트 없이 지수 해소 → "일부 남았다가 처짐"이 공짜로 나옴.
4. **necking·체적 보존 연출**: grab 커널 안에서 당김 방향 신장 + 직교 수축, 신장률을 varying으로
   프래그에 넘겨 얇을수록 밝게(윈도우페인). anchor 반대편에 약한 역변형.
5. **wobble 제거 또는 델타화**: 절대좌표 오프셋 폐기 — grab이 국소 변형을 담당하므로 전신 오프셋 불필요.
6. **상태별 물성**: `RenderParams`에 점탄성 계수 2~3개 추가(τ_creep, 복귀 ζ, 최대 신장) —
   기존 `toRenderParams` 매핑 표에 행만 추가. hungry는 creep 크게(잘 처짐), dormant는 elastic 뻣뻣.
7. 기포 팝 함몰 `-0.3` → 완화(−0.1대) + 링 하이라이트(VISUAL이 약속했으나 미구현인 그것)로 대체.
8. 모든 감쇠는 이미 쓰는 `1-exp(-k·dt)` 패턴 유지 — 주사율 독립.

수치는 전부 **튜닝 시작 가설**이지 과학이 아니다. Motion Lab(breadlab처럼 URL 파라미터 하네스)에서
실기기 비교가 확정한다.

### 4-2b. 기법 근거 (조사 확인분 — 구현 시 참조)

- **감쇠 커널**: quartic smoothstep `x²(2−x²)` 또는 SPH poly6형 `(1−d²/R²)³` — 둘 다 **d²만으로 평가
  (sqrt 불필요)**, 모바일 유리 (iquilezles.org/articles/smoothsteps). **Ricker(멕시칸 햇) 커널**
  `(1−d²/σ²)·exp(−d²/2σ²)`은 중심 양수·주변 음수라 "당긴 곳 솟고 주변 파임(반작용)"이 커널 하나로 공짜.
- **체적 보존 신장**: 당김 축 ×(1+s), 직교 ×1/√(1+s) — det=1. 버텍스 셰이더 단일 패스 실증 논문
  Velocity Skinning (arxiv 2104.04934)의 스케일 행렬 그대로.
- **잔여 변형 + 느린 처짐**: 변형량을 CPU 스칼라 상태로 두고 스프링-댐퍼
  `v -= disp·k·dt; v *= 1−c·dt` (Catlike Coding mesh-deformation 패턴) — §4-2의 elastic/creep 2상태와 동형.
- **찰짐(얇아지며 밝아짐)**: 이웃 샘플링 노멀 재계산의 부산물로 신장률 varying 추출 →
  두께 `t₀/√(1+s)` → DICE 고속 반투명 근사(`H = L + N·distortion; pow(saturate(dot(V,−H)),p)` —
  GDC 2011 Barré-Brisebois) + half-Lambert 랩 라이팅. 라이트 1개 예산 내.
- **정밀도 함정**: 거리 계산은 highp, 색·조명은 mediump(스펙 최소 16bit — 실기기에서만 깨짐).
  현 반죽 셰이더는 precision 선언 금지(three 프렐류드) 관행 유지.
- **A안 보강 증거**: B(CPU 격자)·C(물리 라이브러리) 기각 근거 확정 — ammo.js는 웹 3D 소프트바디
  유일 옵션이나 +750KB gzip·모바일 서멀 스로틀링 보고, cannon-es·rapier·matter.js는 소프트바디
  미지원. BufferAttribute 매프레임 갱신은 61,440 비인덱스 정점 ×vec3 ≈ 737KB/프레임 재복사(실측
  320→24 FPS 급락 사례) — **정점 버퍼 갱신 경로 금지**, uniform만 만진다.
- **상향 탈출구(B-lite)**: A안으로 국소 출렁임·전파가 부족하면 컨트롤 포인트 8~16개만 CPU XPBD로
  돌려 uniform 배열(WebGL1 최소 128 vec4 내)로 밀고 버텍스 셰이더 거리 가중 블렌딩 —
  Ten Minute Physics #12 소프트바디 스키닝의 축소판. 전면 격자 없이 촉감 상한만 올림.
- **기포 순화의 과학적 근거(트라이포포비아 문헌)**: 유발 자극 = "midrange 공간주파수의 고대비
  에너지"(Cole & Wilkins 2013) + **어두운 원형 함몰의 밀집 클러스터 구조 자체**(위상 스펙트럼이
  불편감 분산 24.9% 설명, Pipitone 2020 — 대비만 낮춰선 부족). 반응은 공포가 아니라 혐오
  (Ayzenberg 2018). 카운터: 시각계는 '빛은 위에서' 가정으로 볼록/오목을 판정(Ramachandran 1988) —
  **위쪽 하이라이트 있는 밝은 돔 = 부푼 반죽으로 읽힘**. 설계 규칙: 어두운 테두리·함몰 금지,
  크기 분포 넓게, 비밀집 배치, 넓고 부드러운 이동 스펙큘러(광택 = 신선함 연상, Ye 2020).
  → 현 팝 함몰(−0.3·maxAmp) 완화 방침의 근거이자, `uPoreDensity` 다크 포어 표현도 재점검 대상.

### 4-3. 상태별 유휴 모션 — 현재 3요소(호흡·FBM·기포)에서 확장

현 구조: 상태별 분기 없이 **값만** 바뀜(`renderParams.ts:45-75`), 개체 시드는 `uMoldSeed` 하나.
확장은 "모션 종류 추가"가 아니라 **파라미터 행 추가 + 개체 시드 일반화**가 정합적:

- `uMoldSeed` → `uSeed`로 일반화(개체별 FBM 오프셋·기포 스폰 패턴·호흡 위상). 멀티 르방 전제.
- 장주기 변주: 호흡 진폭에 초저주파(수 분 주기) 모듈레이션 1개 — 반복 티 제거.
- 상태별 신규 연출 후보(전부 기존 채널 재사용): just-fed 젖은 광 정착(`uWet` 이미 있음),
  fake-out(D2, `FAKE_RISE_FACTOR` 이미 있음), peak 압력 맥동(호흡 진폭 미세 랜덤),
  피크 직후 한쪽 처짐(`uCollapse` 커널 비대칭화), dormant 표면 수축(`uCrust` 이미 있음).
- 렌더 랜덤은 게임 상태에 영향 0 유지(GDD §1).

---

## 5. 멀티 르방 + 이름 + 저장 v2

### 5-1. 구조 — 단일 sim 유지, 상위 계층에서 N개 관리 (추천)

sim은 그대로 "르방 1개의 물리"다. 건드리지 않는다. 감싸는 층만 바꾼다:

```
SaveEnvelope v2 (개념)
├─ schemaVersion: 2
├─ savedAt
├─ starters: StarterRecord[]          // 각각 { id, name|null, ordinal, sim: SimState, seed }
├─ activeStarterId
├─ nextStarterOrdinal                 // 삭제해도 번호 재사용 안 함
├─ shared:                            // 전역 소유
│   ├─ inventory (재료함 — Phase 6)
│   ├─ discoveredVariants (변형 레시피 발견)
│   └─ collection?                    // ← 결정 필요: 도감을 전역으로 승격할지 (5-4)
├─ settings / flags (기존)
```

- `gameStore`는 `activeStarterId`가 가리키는 sim 하나만 advance/dispatch. 비활성 르방은
  **닫힌 함수 모델 덕분에 그냥 둬도 된다** — 전환 시 그 sim의 `advance(now)` 1회면 끝. 백그라운드 시뮬 0.
- 시계 역행 재정박은 **전 starter 순회** — `reanchor`를 N개에 적용. `tests/clock.test.ts` 확장 필수.
- 개체 시드 `seed = createdAt` 재사용(현 `uMoldSeed` 관행 그대로).

### 5-2. 마이그레이션 v1→v2 (순서 폭탄 해제 포함)

1. **먼저** `parseEnvelope` 순서를 `migrate` → `validateAndClamp`로 뒤집는다
   (`persistence.ts:167-171` 주석이 예고한 그 작업). `app.ts:105-108` 임포트 경로도 동일하게.
2. `MIGRATIONS[1]`: 기존 단일 `sim`을 `starters[0]`으로 감싸기.
   - `name = 기존 label` (있으면 이월 — 이름을 잃지 않는다), `ordinal = 1`, `id` 새로 발급.
   - `activeStarterId = 그 id`, `nextStarterOrdinal = 2`.
   - `collection`·`flake`·`pendingBake` 무손실 이월.
   - 기존 빵 기록에 starterId 없음 → `starters[0].id`를 기본값으로.
3. 검증 v2: starter 배열 항목 단위 관대 처리(불량 항목만 폐기, 전체 null 금지 — `collection` 항목
   처리 관행 `persistence.ts:76-91` 따름). 단 `starters`가 0개가 되면 null(새 게임).
4. 픽스처 테스트: v1 실저장본 → v2 왕복, 이름·도감·flake 보존 assert.

### 5-3. 이름 — label 충돌 해소

현재 label = **5단계(45사이클·30일) 보상**이며 직접 입력(`actions.ts:147-152`, 테스트 0건).

| 안 | 내용 | 판정 |
|---|---|---|
| 원안(사용자) | 처음부터 이름 짓기, 기본 "르방이 N" | 5단계 보상 증발 |
| **개선안(추천)** | `name`은 **처음부터 자유**(생성 시 입력 또는 스킵→"르방이 N"). 5단계 보상은 **병 각인/칭호**로 전환(이름 옆 장식 표기) | 보상 보존 + 요구 충족 |

- 기본 이름: `name`이 null/공백이면 표시 시점에 `르방이 {ordinal}` — **저장하지 않고 파생**(닫힌 함수 정신).
- ordinal은 생성 순 증가, 삭제해도 재사용 안 함(`nextStarterOrdinal`).
- 이름 변경 즉시 가능(설정 아니라 관찰 카드/홈에서). 12자 제한 유지.
- `setLabel` 게이트 리터럴 `5`(`actions.ts:148`) → 상수화 + 테스트 추가(D6·D7 해소를 이 작업에 편승).

### 5-4. 소유권 표 (권장 기본값)

| 개별(starter) | 전역(shared) |
|---|---|
| sim 전체(발효·급여·위치·산미·maturity·mass) | 재료함 inventory |
| flake(건조 백업) | 발견한 변형 레시피 |
| name·ordinal·seed | 설정·구매 권한 |
| — | **도감(collection)**: v2에서 전역 승격 추천 — "빵을 구운 기록"은 집의 것. 단 기록에 `starterId` 필드 추가(어느 르방으로 구웠나) |

주의: collection 전역 승격은 `discardStarter`/`restoreFlake`의 "도감 보존" 로직(`actions.ts:168-196`)을
단순화한다(원래 보존하려고 애쓰던 것이 자연히 전역이 됨).

### 5-5. 스와이프 제스처 — 소유권 FSM

현 `input.ts` FSM: 탭(<8px/<250ms)/홀드(0.8s)/스터(≥8px). 좌우 스와이프와 충돌한다.

권장 규칙 (한 포인터 세션의 소유권은 중간에 안 바뀜):

- **반죽(항아리) 위에서 시작** → 기존 FSM 그대로 (grab/stir/poke). 스와이프 아님.
- **배경(항아리 밖)에서 시작 + 수평 우세(|dx|>|dy|·1.5) + 이동 ≥48px** → 르방 전환.
- 르방 1개면 스와이프 비활성. 모달·급여 연출·쇼케이스 중 전환 차단.
- 접근성 대체: 헤드 칩에 `‹ 르방이 2 · 2/3 ›` 이전/다음 버튼 — 스와이프 없이도 도달 가능.
- 전환 연출: 씬 하나 재사용(SceneHost 신규 생성 금지) — 파라미터 `snapParams`로 즉시 스냅 +
  캔버스 좌우 슬라이드는 CSS 트랜지션(캔버스 자체 이동 아니라 스냅샷 페이드 0.25s면 충분).
  WebGL 컨텍스트 1개 유지.

### 5-6. 알림 — 스팸 방지

현 구조: 고정 슬롯 3개(id 1/2/3), `getPending()` 전량 취소 후 재예약. N르방이면 3N개 = 스팸.

| 안 | 판정 |
|---|---|
| starter별 id namespace (id = ordinal×10+slot) | 알림 수가 N배 — 탈락 |
| 가장 긴급한 르방만 | 정보 손실 — 두 번째 르방 죽음 |
| **집계(추천)** | 슬롯 의미는 유지하되 **시각별 병합**: 전 starter의 planNotifications를 합쳐 같은 슬롯이 여러 개면 가장 이른 시각 1건 + 문구를 집계형으로("르방이 2개가 밥을 기다리고 있어요"). 하루 상한 2건 유지 |

- 순수 함수 확장: `planNotificationsAll(starters, now)` — 기존 `planNotifications` N회 호출 후 병합.
- 죽음·곰팡이 확정 푸시 0건 규칙 유지(GDD §7).
- 냉장 주간 반복은 냉장 르방이 1개라도 있으면 1건.

### 5-7. 슬롯 수·과금

- 무료 기본 **3슬롯** (통밀/호밀 별도 항아리 운용까지 무료로 가능해야 밥주기 세분화가 산다).
- 추가 슬롯은 IAP 후보(§경제). **기존 사용자의 첫 르방을 잠그거나 삭제하는 구조 금지.**
- 저장 크기: SimState ≈ 15필드 · 슬롯 10개여도 localStorage에 무해. 상한은 UX 문제(스와이프 피로)로
  결정 — 최대 5~7 권장.

---

## 6. Levain Lab (개발자 도구) — "내 계정 해금"의 대체

계정·백엔드 0이므로 "내 계정만"은 구현 불가(앱 fetch 1곳 = OTA GET뿐). 3안 비교:

| 안 | 판정 |
|---|---|
| **A. DEV 빌드 전용 Levain Lab (추천)** | Vite `import.meta.env.DEV` 게이트 — 프로덕션 번들에서 **트리쉐이킹으로 코드 자체가 제거**. 유출 불가능 |
| B. 프로덕션 숨은 메뉴 | 발견 리스크 + 코드 잔존. 탈락 |
| C. 계정·서버 entitlement | 백엔드 신설 — 이 요구 하나로는 과잉. 탈락 |

Lab 필수 기능 (기존 자산 재사용이 대부분):

- **FakeClock 주입** — 이미 있다(`platform/clock.ts`, 테스트 전용 → Lab에 노출). 시간 앞뒤 이동.
- **시나리오 픽스처**: SimState는 15필드 순수 데이터 — 상태별 프리셋 JSON이면 끝.
  프리셋 목록: 단계 0~5 / D2 fake-out / 잠잠기 / rising / near-peak / peak / falling / hungry /
  hooch / sour / fridge / dormant / revive 1회차 / spot / spread / moldy / flake 유무 / kahm.
  전부 **타임스탬프 역산으로 생성 가능**(닫힌 함수라 `lastFedAt = now − X h`만 넣으면 그 상태).
- 멀티 르방 프로토: 프리셋 N개를 starters로 깔면 스와이프로 전 상태 순회 — 사용자가 원한
  "각 단계별 르방 동시 확인"이 이것.
- 전체 레시피·재료 지급, 재료 수량 조작.
- 알림 플랜 미리보기(`planNotifications` 출력 덤프).
- 렌더 파라미터·모션 프리셋 전환(Motion Lab 겸용 — §4 튜닝 하네스).
- v1→v2 마이그레이션 미리보기(픽스처 → parseEnvelope 결과 덤프).
- **일반 저장 `levain:save`와 분리**: Lab은 `levain:lab-save` 키 사용. 내보내기 금지.
- 수용 기준: 프로덕션 빌드 산출물에서 Lab 문자열·전역 객체 grep 0건.

기존 `window.__levainScene`(DEV 계기판)·breadlab 하네스 관행과 같은 축 — 새 발명 아님.

---

## 7. 밥주기 세분화

### 7-1. 3안 → **B안(프리셋 + 상세 펼치기) 추천**

| 안 | 판정 |
|---|---|
| A. 현행 비율 프리셋만 | 이미 있음(3종). 요구 미충족 |
| **B. 쉬운 프리셋 + 상세 펼치기 (추천)** | 기존 모달 확장. 코지 톤 유지 |
| C. 그램 입력 실험실 | 트래커 앱(Surdeg 등)과 같은 축 — 차별성 파괴. 탈락 |

쉬운 모드(현 모달 확장): 빠른 밥 1:1:1 / 반나절 1:2:2 / 자기 전 1:5:5(기존) + **신규 2행**:
"냉장 갈 준비"(밥+냉장 이동 묶음 — 현재 2탭을 1탭으로) / "깨우기"(휴면 회복 — 현 1:1:1 직행 유지).

상세 펼치기(성숙 4단계+ 해금): **밀가루 종류**(다목적/통밀/호밀 — v2 신규 축) + 예상 피크 **범위**
표시("저녁 7시쯤 — 창가면 더 일러요"). 그램 입력·수분율 조정은 **보류**(v3) — SimState에
hydration 필드가 없고(100% 고정, GDD §2), 축 하나(밀가루 종류)만으로 호밀빵 산미 시너지 등
충분한 깊이가 나온다.

### 7-2. 밀가루 종류 — 최소 침습 설계

- SimState +1 필드 `flour: 'white' | 'wholewheat' | 'rye'` (기본 white, 구세이브 키 부재→white —
  label 패턴).
- 효과(전부 기존 축의 배율): 통밀·호밀 = 발효 가속(유효시간 배율 ×1.15~1.25) + 산미 적립 가속 +
  색 톤 시프트(렌더). 호밀빵·통밀 깜빠뉴 판정에 가산점(해당 flour면 sourFit 보정).
- 수치는 현실 조사(§14) 도착 후 확정. "정답 시간" 단정 금지 — 관찰 카드는 범위로 말한다.
- stiff/liquid starter 물성은 **모션 파라미터로만**(§4-2-6) — sim 축 추가 안 함(v3 보류).

### 7-3. 안전 정정 (기획서 확정 사항)

- **후치 ≠ 곰팡이**: 이미 코드가 분리(hooch 파생 + kahm 파생 + mold 3단계). 문구만 점검 —
  후치는 "배고픔 신호, 밥 주면 됨"으로 유지(현 `copy.ts` 정합).
- **확정 곰팡이 부활 금지**: 현 규칙이 이미 옳다 — moldy는 종착, 급여 회복 불가,
  폐기/flake 복원만(`actions.ts:199-202`). **광고·결제로 "동일 배양종 되살리기"를 팔지 않는다** —
  파는 것은 "건조 백업 복원 보조"와 "새 배양 계보 이어받기"뿐(§9).
- 반점·확산 단계 급여 회복은 **게임 추상화**로 명시(실제 식품 진단 아님) — 스토어·도움말에
  실제 곰팡이 판단 기준을 싣지 않는다(의료·식품 조언 회피).
- 생반죽 섭취 암시 금지 — 문구 감수 항목에 추가.

---

## 8. 재료함·변형 레시피·빵 도감

### 8-1. 진입 UX — 3안 → **명시 세그먼트 + 재탭 보조(추천)**

| 안 | 판정 |
|---|---|
| A. 재탭 토글만 | 숨은 동작 단독 의존 — 발견 불가. 탈락 |
| **B. 레시피 화면 상단 세그먼트 `레시피 | 재료함 | 빵 도감` + 재탭 보조 (추천)** | 항상 보이고, 파워유저 단축도 충족 |
| C. 별도 하단 탭 | 탭 2개 잠금(ARCH §5) 파괴. 탈락 |

재탭 상태 전이(현재 재탭 = `setRoot` 전체 재마운트뿐):

| 현 상태 | 레시피 탭 재탭 | 
|---|---|
| 다른 탭 | 레시피 루트(마지막 세그먼트) |
| 레시피 하위 상세(쇼케이스 등) | 루트 복귀 |
| 루트·레시피 세그먼트 | 재료함 세그먼트 토글 |
| 루트·재료함 세그먼트 | 레시피 세그먼트 토글 |

첫 2~3회 "한 번 더 누르면 재료함" 힌트 토스트. **선행 조건: BUG-1(setRoot display 미복구) 수정.**

### 8-2. 데이터 모델 (개념)

- `IngredientDef { id, name, forms: IngredientForm[] }` — 올리브(과육/슬라이스/오일/브라인),
  초코(코코아/칩/필링), 딸기(생과/구운/동결건조/잼), 밤(구운 조각/밤가루/퓌레).
- `CompatibilityRule { baseRecipeId, ingredientId, form, status: verified|conditional|experimental|blocked, sourceRef }` —
  **호환성은 데이터가 결정, UI if문 금지.**
- `VariantDef { id, baseRecipeId, ingredients: {id, form}[], assetId }` — **`assetId` 분리**:
  초기 변형은 기본 GLB 재사용 + 카드 배지·색 연출만. 전용 GLB는 검증된 인기 변형만 추후.
- `Inventory: Record<ingredientId, count>` — 전역(§5-4). 소프트캡 9, 초과분은 교환 재화로.
- **재료는 순수 컬렉팅 축 (사용자 확정 2026-08-24)**: 재료·변형은 **sim에 일절 영향 없음** —
  밥주기·발효·산미·굽기 판정은 기존 그대로. 재료의 역할은 오직 **변형 레시피 해금 키 + 도감 수집**.
  §18-4 물리 영향표는 sim 데이터가 아니라 **카드 설명·카탈로그 근거용 메타데이터**로만 사용.
- 해금 공식: `기본 해금 + 재료 형태·수량 + verified 규칙 → 원자적으로 (재료 차감 + 변형 발견)
  동시 커밋`. **무효 조합은 시도 전 차단 — 재료 소비 0.**
  한 번 발견한 변형은 재굽기에 재료 재소비 없음(mass만 소모, 기존 굽기 경제 유지).
- 카탈로그는 "검증 카탈로그 v1(기준일)"로 유한 정의 — §14 조사 결과가 채운다.
  근거 없는 조합은 blocked(="현 카탈로그 미검증", 세상에 없다는 뜻 아님).

### 8-3. 빵 도감 — 감상 경로 신설

현 문제(감사 확정): 완성 카드 재탭 = **무조건 재굽기 confirm** — 감상 경로 0.

추천 흐름: 완성 카드 탭 → **상세 화면**(신규, router.push) —
큰 썸네일 → "3D로 보기"(기존 `enterShowcase(url)` 재사용) / 기록(첫 완성일 `firstAt` — 저장은 되는데
표시 안 하던 것, 최고 등급, 횟수, 사용 르방) / 변형 목록(해금·힌트) / **"다시 만들기"** 버튼(기존
굽기 confirm으로). 미완성 카드는 현행 유지(바로 굽기 flow).

필터 칩: 전체 / 지금 만들 수 있음 / 완성 / 즐겨찾기. 잠긴 빵은 해금 힌트만(결제 유도 0).
메인(항아리) 화면에 빵 갤러리 금지 — 마켓팅 잠금 유지.

---

## 9. 무료 경제 (광고 이전에 먼저)

광고 없이도 모든 핵심 콘텐츠가 장기적으로 열리는 바닥을 먼저 깐다. 광고는 "가속"만 판다.

- 재화 2종만: **재료** + **교환 가루**(중복·초과분 전환용 단일 무료 재화). 유료 젬 없음.
- 무료 획득 경로: 첫 변형용 재료 1개 선택 지급(온보딩) / 성장 단계 달성 / 첫 빵 완성 /
  주간 돌봄 목표(실패해도 리셋 없는 누적 미션) / 중복 교환.
- 재료 소프트캡 9, 초과분 자동 교환 가루 전환. 만료 없음. 무효 조합 소비 0(§8-2).
- **핵심 레시피(기본 10종) 중 광고/IAP 전용 0개** — 수용 기준.

## 10. 광고 경제 (조사 근거 반영, 기준일 2026-08-24)

### 10-1. 정책 사실 (공식 문서 확인분)

- 보상형 광고는 **명시적 opt-in 버튼 의무**, 매 게재 전 "무엇을 하면 무엇을 받는지" 고지,
  거절 시 불이익 금지 (AdMob 7313578 · 2753860).
- SSV(서버 검증)는 **콜백 받을 서버가 있어야** 가능 — 백엔드 0이면 클라이언트 보상뿐.
  정책 위반 아님, 조작 피해가 본인 귀속인 오프라인 싱글게임에선 수용 가능한 리스크(비즈니스 판단).
- 광고 SDK 도입 = **Data Safety 폼 전면 갱신 + AD_ID 권한 + 처리방침 전면 개정**(제3자·식별자·
  철회 경로·앱 내 링크). 현 "수집 없음" 선언과 정면 충돌 — PRIVACY.md 개정은 도입의 전제.
- **타깃 연령에서 아동 제외**가 Families 광고 제한(자체 인증 SDK 한정 등)을 통째로 회피하는 길.
  코지 아트가 아동 취향으로 읽히지 않게 스토어 톤 점검.
- **네이티브 SDK는 OTA 불가** — 새 AAB + 심사. 이후 배치·수치 조정은 웹 레이어라 OTA 가능.
  → "네이티브 훅 먼저 싣고 웹 feature flag로 켜는" 구조가 심사 횟수를 최소화.
- 플러그인: `@capacitor-community/admob` v8(Cap 8 + UMP 동의 + SSV 옵션) — 사실상 단일 선택지.

### 10-2. 슬롯 설계 (전부 사용자 선택형 보상 광고만)

| 슬롯 | 내용 | 하루 상한(가설) |
|---|---|---|
| 재료 배송 | 레시피 계열 선택 → 후보·신규 보장 표시 → 광고 → 재료 1 | 3회 |
| 회복 도움 | 후치·휴면 회복 대기 단축 1단계 | 1회 |
| 레시피 힌트 | 막힌 변형의 재료 힌트 | 1회 |
| 건조 가속 | flake 제작 즉시 완료류 | (통합 상한 내) |

전체 하루 5회·세션 2회·연속 2회 후 10분 쿨다운 — **A/B 시작 가설로 명시, 확정값 아님**.

금지(설계 불변): 앱 시작/전면 강제 광고 · 항아리 화면 광고 · 급여 인질 · 곰팡이 연출 직후 압박 ·
no-fill/취소 시 횟수 차감 · 무료 회복 경로 제거 · 설치/클릭 요구 · 보상의 광고 연쇄.
**되살리기(사망) 광고 금지** — §7-3. 건조 백업·새 시작은 영원히 무료.

보상 지급 멱등성: 지급 원장에 `{slotId, date, adUnitId, grantedAt}` 로컬 기록 — 같은 노출 중복 지급 0.

## 11. 결제 경제

### 11-1. ₩1,900 재료 번들 — **확률 공개형 랜덤 (사용자 확정 2026-08-24)**

사용자 결정: 랜덤 유지 + 확률 공개로 정면 대응. 따르는 의무·설계:

**확률 공개 의무 3종 (전부 이행)**
1. 게임산업법 §33-2: 확률형 아이템 종류·종류별 확률을 **게임 내(구매·조회 화면) + 인터넷
   홈페이지(검색 가능한 형태) + 광고물("확률형 아이템 포함" 문구)**에 백분율로 표시. 시행 2024-03-22.
   영세사업자 예외(3년 연평균 매출 1억↓) 해당 여부는 별도 전문가 검토 — **해당해도 표시하는 쪽으로
   설계**(스토어 정책은 예외 없음).
2. Google Play: "구매 직전·근접 위치"에 확률 표시.
3. App Store 3.1.1: 구매 전 아이템 유형별 확률 공개. (iOS는 비범위지만 규칙은 동일 설계에 반영)

**보호장치 (전부 탑재)**
- 번들 내 중복 없음 · 현재 열린 베이스 레시피에 쓸 수 있는 재료만 풀에 포함 · 보유 상한(소프트캡 9)
  도달 재료는 풀에서 제외 후 **확률 재정규화** — 재정규화 후의 "실제 개인 확률"을 구매 직전 표시.
- 신규 재료 최소 1개 보장(가능한 경우 — 불가능하면 그 사실 표시).
- **천장**: 3번째 번들마다 재료 선택권 1회. 천장 카운터는 저장 envelope에 보존.
- 확률 설정에 `economyConfigVersion` 부여 — 변경 이력 추적, 공개 페이지와 동기.
- **백엔드 0 리스크 명시**: 결과 생성이 클라이언트 랜덤 — 조작 피해는 조작자 본인 귀속(오프라인
  싱글). 천장 카운터는 로컬 저장이라 **재설치·기기 변경 시 유실 가능** — 공개 페이지·상품 설명에
  이 한계를 정직하게 기재. 유실 불만이 실측되면 그때 백엔드(§12-B) 재검토.
- 확률 공개 페이지는 기존 Vercel(levain-ota 계열)에 정적 1장 — 신규 인프라 0.

**대안으로 보류**: 선택 꾸러미(확률 의무 0) — 랜덤 반응이 나쁘면 전환 쉬움(역방향보다 싸다).

### 11-2. ₩900 상품군

- "곰팡이 부활" **판매 금지**(§7-3). 대체: 후치·휴면 **응급 회복 키트**(무료 절차 단축) /
  새 배양 **계보 이어받기**(이름·도감·추억 이월 — discardStarter는 지금 label을 버린다, 이건 유료가 아니라
  기본으로 이월하도록 바꾸는 게 옳음 → 유료는 "즉시 성숙 보정" 정도로) / 건조 백업 **즉시 재활성화**.
- 무료 경로를 항상 먼저 표시. 카운트다운·한정 할인 압박 금지.

### 11-3. 건조 보관 — 종량제 대신 **선반 슬롯 영구 해금**

기본 flake 1슬롯 무료(현행). `건조 선반 +1` = **non-consumable ₩900~1,900**, 구매 복원 가능,
재활성화 무료. 환불돼도 보관 데이터 삭제 금지(슬롯만 잠금).

### 11-4. 기타 IAP

권장: 광고 제거(₩4,400대) · 추가 르방 슬롯 · 병 장식(유리·천·고무줄·각인 — 무캐릭터 유지) ·
계절 장식 고정 팩 · 개발자 응원 팩.
초기 제외: 구독 · 시즌패스 · 핵심 레시피 유료화 · 재료 만료 · 인벤 압박 후 용량 판매 ·
사망 직전 특가 · 동적 가격. 가격 문자열 하드코딩 금지 — 스토어 현지화 가격 API.

### 11-5. ⏰ Billing v8 기한

**2026-08-31부터 신규 앱·업데이트 전부 Billing Library v8+ 의무**(연장 시 11-01). 기준일에서 7일.
무료 플러그인(@capgo/native-purchases 등)은 README 기준 Billing 7.x — 도입 시점에 실제
build.gradle 버전 확인 필수. **RevenueCat**(@revenuecat/purchases-capacitor)은 서버 검증·entitlement를
백엔드 대행 — 자체 서버 0으로 서버 검증을 얻는 유일한 경로, 유력 후보.
→ **결제는 절대 서두를 단계가 아니다**: 지금은 어차피 신규 도입이라 v8 이슈는 "도입 시점의
플러그인 선택 기준"일 뿐, 오늘 뭘 해야 하는 마감이 아님.

## 12. 백엔드 판단

| 안 | 판정 |
|---|---|
| **A. 완전 로컬 유지 + 최소 통합 (추천)** | 광고=클라이언트 보상(SSV 포기), 결제=RevenueCat 대행 또는 클라이언트 acknowledge+queryPurchases 회수. 랜덤 상품 안 만듦 |
| B. 익명 계정+commerce 백엔드 | 랜덤 상품·천장 보존·기기 이전이 필요해질 때만 |
| C. 풀 계정·클라우드 동기화 | 현 제품 정체성(완전 로컬) 파괴. 보류 |

A안 필수 처리: 구매 직후 acknowledge/consume(3일 미처리 = **자동 환불**) + 앱 시작마다
`queryPurchases` 회수 + pending 상태에서 지급 금지 + 오프라인 시 돌봄·기존 레시피·도감 전부 정상.

## 13. 개인정보·연령·정책 (광고/IAP 도입 시)

- PRIVACY.md 전면 개정 + 앱 내 처리방침 링크 신설(현재 없음 — 설정 모달에 링크 항목 추가).
- STORE_LISTING.md의 "광고 없음·인앱 결제 없음" 문구 제거 + Data Safety 재작성.
- 타깃 연령: **아동 미포함** 선언 권장. 연령 미확인 사용자에게 확률·과금 실험 금지.
- 확률형 상품 0이면 게임산업법 §33-2·스토어 확률 공개 의무 미발생. 랜덤 도입 시 게임물관리위
  문의/법률 자문 — 법적 결론은 이 문서가 내리지 않는다.
- 이름·메모를 분석 로그로 전송 금지(분석 자체가 아직 0이지만 도입 시 원칙).

---

## 14. 핵심 결정 레지스터 (사용자 확정 4 + 파생)

| ID | 결정 | 추천 | 근거 | 되돌리기 비용 |
|---|---|---|---|---|
| DEC-1 | 촉감: 셰이더 점탄성 vs 물리엔진 | **A. 셰이더 2시간척도 확장** | 원인이 grab anchor 부재로 특정됨(§4) — 구조 교체 불필요. draw call·컨텍스트 불변 | 낮음(uniform 추가뿐) |
| DEC-2 | 재료함 진입: 숨은 재탭 vs 명시 세그먼트 | **명시 세그먼트 + 재탭 보조** | 숨은 동작 단독 = 발견 불가. BUG-1 수정 선행 | 낮음 |
| DEC-3 | ₩1,900: 랜덤 번들 vs 선택 꾸러미 | **랜덤 + 확률 공개 (사용자 확정)** | 확률 공개 3종 의무 전부 이행 + 보호장치(중복 없음·신규 보장·천장·재정규화 확률 표시). 클라이언트 랜덤 리스크는 명시 수용 | 랜덤→선택 전환은 쉬움(안전 폴백 보유) |
| DEC-4 | 개발 권한: 계정 vs Levain Lab | **DEV 빌드 Levain Lab** | 백엔드 0 현실. 트리쉐이킹으로 프로덕션 유출 구조적 차단 | 낮음 |
| DEC-5 | 이름 vs 5단계 label 보상 | name 자유화 + 보상은 각인/칭호로 전환 | §5-3 | 중간(문구·보상 재설계) |
| DEC-6 | 도감 소유권 | 전역 승격 + 기록에 starterId | §5-4 | 마이그레이션 1회 |
| DEC-7 | 광고·IAP 도입 자체 | **Phase 8~9로 후치** — 정책·방침·AAB 동반 대공사. 먼저 무료 경제로 재미 검증 | §10-1 | — |

## 15. 로드맵 (구현 순서 — 각 Phase 독립 배포 가능)

| Phase | 내용 | 산출물 | 완료 기준 | 난이도 |
|---|---|---|---|---|
| **0** | 문서 충돌 확정 + 버그 2건 | STORE_LISTING/VISUAL/GDD 드리프트 21건 정리, BUG-1·BUG-2 수정 | 드리프트 표 소거, 재현 절차 통과 | 하 |
| **1** | Motion Lab + 촉감 개편 | grab 채널·2시간척도·기포 순화, DEV 하네스 | §4-1 목표 장면 실기기 촬영 비교 | 중상 |
| **2** | 저장 v2 + 멀티 르방 코어 | migrate 순서 반전, StarterRecord[], 전환 | v1 픽스처 무손실 왕복 테스트 | 중상 |
| **3** | 이름·스와이프·집계 알림 | name/ordinal, 제스처 FSM, planNotificationsAll | §16 수용 기준 | 중 |
| **4** | 밥주기 현실화 | 프리셋 확장 + flour 축 + 관찰 카드 범위 표현 | 조사 근거 반영 확인 | 중 |
| **5** | Levain Lab | 시나리오 픽스처 · FakeClock · 프리셋 스와이프 | 프로덕션 번들 grep 0건 | 중 |
| **6** | 도감 상세·재료함·변형 공식 | 세그먼트 UI, CompatibilityRule 데이터, 카탈로그 v1 탑재 | 무효 조합 소비 0·원자 해금 테스트 | 상 |
| **7** | 무료 경제 | 미션·교환 가루·소프트캡 | 광고 0으로 전 변형 도달 가능 검증 | 중 |
| **8** | 보상형 광고 (feature flag) | AdMob 플러그인 + 방침·Data Safety 개정 + **새 AAB** | opt-in·no-fill·멱등성 수용 기준 | 상 |
| **9** | IAP | 선택 꾸러미·선반 슬롯·광고 제거. RevenueCat 검토 | acknowledge·복원·pending 수용 기준 | 상 |
| **10** | 다중 재료 상위 레시피·계절 | 카탈로그 v2 | — | 중 |

Phase 0~2가 전제 사슬: **버그·마이그레이션 순서를 안 잡으면 그 위 전부가 모래성**.
1과 2는 병렬 가능(렌더 vs 저장 — 파일 겹침 없음).

### 15-1. 위임 정책 (사용자 확정 — 단순작업은 전량 하위모델)

| 작업 성격 | 담당 |
|---|---|
| 기계적: 픽스처 생성, 카탈로그 46행 데이터 전사, 문서 드리프트 반영, 테스트 보일러플레이트, 이미지 프롬프트 JSON 작성, copy.ts 문구 초안 | **Sonnet 서브에이전트** (Haiku 사용 금지 — 사용자 확정) — 본세션은 계약(스펙·형식)만 주고 전량 리뷰 |
| 판단: 마이그레이션 설계, 제스처 FSM, 셰이더 커널 튜닝, 경제 수치, 문구 감수 확정 | 본세션 직접 |

빵 GLB 배치 때 검증된 패턴(스펙 먼저 → 서브에이전트 전사 → 본세션 코드 리뷰) 그대로.
서브에이전트 반복 상한은 프롬프트 지시가 아니라 스크립트 층에서 집행(핸드오프 교훈).

### 15-2. 변형 레시피 이미지 프롬프트 (Phase 6 산출물 — 그록 일괄 투척용)

기존 파이프라인 정본 그대로: `assets/prompts/README.md`(v2 절차) + `assets/prompts/style-shared.json`
(shared-style-v2) + `assets/prompts/breads/<id>.json` 양식.

- 신규 변형 레시피마다 `assets/prompts/breads/<variantId>.json` 1개 — **기존 v2 스키마 필드 동일**:
  `id/version/name_ko/subject/geometry(silhouette·crust)/style_ref: "shared-style-v2"/camera/background/
  lighting/negative/views/output(1:1 1024)/prompt_flat/followup_views(2)/notes_ko`.
- prompt_flat은 자기완결 문단(복붙 1회), followup_views는 같은 채팅에서 front elevation·top-down.
  배경 페일 세이지 #DFEAE0·matte·그림자 금지·재료별 negative(팬케이크의 시럽처럼 변형 재료가
  유발할 기본 토핑 명시 차단) 준수.
- 변형 특화 규칙: 베이스 빵 지오메트리 서술은 해당 베이스 JSON에서 계승하고, **재료 표현만 추가**
  (올리브 슬라이스 박힘, 초코칩 스터드, 딸기 스월 등) — §18 카탈로그의 형태 판정과 일치시킬 것.
- 작성 주체: 하위 서브에이전트(§15-1) — 기존 10종 JSON을 읽고 형식 모사, 본세션이 리뷰.
- 산출 시점: 카탈로그의 verified 변형이 확정되는 Phase 6. 사용자가 **한 번에 그록에 던질 수 있게**
  변형 전량을 같은 폴더에 모아 두고, README에 변형 목록 추가.
- 단, §8-2 결정대로 **초기 변형은 기본 GLB 재사용 + 카드 연출**이 기본 — 이미지 프롬프트는
  "전용 자산으로 승격할 변형"(인기·대표 변형)부터 순차 생산. 전량 선생산 아님.

## 16. 수용 기준 (발췌 — 구현 Phase별 테스트로 옮길 것)

저장·멀티: v1 저장본이 이름(label→name)·도감·flake 무손실 이전 / 개별 르방 상태 불혼합 /
빈 이름 = "르방이 N" 안정 표시 / 삭제 후 ordinal 재사용 0 / 역행 시 전 starter 재정박.
제스처: 반죽 내 드래그와 배경 스와이프 상호 불간섭 / 버튼으로도 이전·다음 이동 / 1개면 스와이프 무효.
모션: 60/120Hz 동일 복귀 곡선 / 기본 액체 르방 즉시 튕김 없음(오버슈트 ≤1회·≤5%) /
가시 기포 동시 2~4 상한 / 모션 감소 설정 존재(§4-3, prefers-reduced-motion 현재 0건).
레시피: 무효 조합 재료 소비 0 / 해금+차감 원자성 / 호환성은 데이터 결정 / 굽지 않고 재감상 가능 /
재료함은 재탭 없이 발견 가능.
경제: 광고는 명시 선택으로만 시작 / 취소·no-fill 무차감 / 동일 노출 중복 지급 0 / 무료 회복 상존 /
확정 곰팡이 부활 암시 0 / 핵심 레시피 유료 전용 0 / 가격은 스토어 현지화 / 환불로 데이터 삭제 0 /
네이티브 SDK = 새 AAB(OTA 완결 주장 금지).
알림: 다중 르방 알림 하루 상한 2건 유지 / 사망 푸시 0건.

## 17. 비범위 (이번 사이클에서 안 함)

iOS · 클라우드 동기화 · 소셜(분양 링크는 백로그) · 그램 입력 실험실 · 수분율 sim 축 ·
자동 갱신 구독 · 시즌패스 · 유료 랜덤 상품(백엔드 결정 전) · 분석 SDK(광고 도입 전까지 0 유지).

---

## 18. 레시피 검증 카탈로그 v1 (기준일 2026-08-24) — 조사 완료

조사 방법 명시(전수조사 아님 — 유한 카탈로그): 베이스 10종 공식은 King Arthur Baking 본문
그램 단위까지 직접 확인(★), 조합 판정 46행은 실제 공개 레시피 URL 검색 확인. 등급:
**verified**(직접 실증 URL) / **conditional**(비-사워도우·형태 제한·비율 상한부 실증) /
**experimental**(근거 미발견, 원리상 성립) / **blocked**(현 카탈로그 검증 실패 — "세상에 없다" 아님).

**집계: verified 24 / conditional 16 / experimental 5 / blocked 1.**

### 18-1. 판정 원칙 (게임 데이터 구조를 결정)

**재료가 아니라 형태에 등급을 매긴다.** 호환성 서열이 형태별로 갈린다:
- 올리브: 오일 > 과육 > 토핑 > 브라인
- 딸기: 동결건조 > 구운 과일 > 잼 > 생과 (동결건조 = 수분 0, 전 베이스 최고 호환)
- 밤: 구운 조각 > 밤가루(무글루텐 — **상한 20~25% 하드캡**) > 퓌레(필링 전용)
- 초코: 칩(입자, 국소) / 코코아(흡수, 4~8% 상한) / 필링(로프·바브카 전용)

### 18-2. 사용자 예시 검증 결과

| 주장 | 판정 |
|---|---|
| 올리브×포카치아 가능 | ✅ verified (사워도우 직접 실증) |
| 올리브×깜빠뉴 가능 | ✅ verified |
| 올리브×크래커 가능 | 🟡 **오일 형태만** verified, 과육 혼입은 experimental |
| 올리브×식빵 가능 | 🟡 오일 verified, 과육 conditional |
| **올리브×팬케이크 불가** | ❌ **반증** — 올리브 세이버리 팬케이크·사워도우 세이버리 팬케이크(KAB ★) 각각 실증. 정당 판정 = experimental. **"세이버리 팬케이크" 경로를 게임에 추가하는 게 근본 해법** |

blocked는 단 1건: **크래커×초코칩**(1.5mm 압연에서 칩 용융·탄화 — 근거·원리 모두 불리).

### 18-3. 판정표 하이라이트 (전체 46행은 조사 원문 — 구현 시 데이터 파일로 전사)

verified 대표: 올리브 과육×{포카치아·깜빠뉴·호밀·통밀}, 올리브유×{포카치아·크래커·식빵·깜빠뉴},
초코칩×{팬케이크·스콘}, 코코아×{팬케이크·크래커·스콘·포카치아(KAB 더블초코 ★)·깜빠뉴·호밀·통밀},
초코 필링×식빵(KAB 사워도우 바브카 ★), 딸기 생과×{팬케이크·스콘}, 딸기 동결건조×깜빠뉴,
딸기 잼×깜빠뉴(스월), 딸기 구운×포카치아, 밤 조각×{깜빠뉴·바게트·통밀}, 밤가루×호밀.

### 18-4. 재료 형태 → 물리 영향 (게임 데이터 모델, -2~+2)

| 형태 | 수분 | 염분 | 당 | 지방 | 발효속도 | 특기 |
|---|---|---|---|---|---|---|
| 올리브 과육 | +1 | +1 | 0 | +0.5 | −0.5 | 혼입 상한 ~20%, 소금 감량 |
| 올리브유 | 0 | 0 | 0 | +2 | 0 | 크럼 연화, 5~8% 표준 |
| 올리브 브라인 | +1 | **+2** | 0 | 0 | −1 | 소금 자동 차감 필요 |
| 코코아 | −1 | 0 | 0 | +0.5 | −0.5 | 글루텐 희석, 수분 +5%p 보정 |
| 초코칩 | 0 | +1 | 0 | +1 | 0 | 표면 탄화 위험 |
| 딸기 생과 | **+2** | 0 | +1 | 0 | +0.5 | 크럼 손상 위험 최대 |
| 딸기 동결건조 | −0.5 | 0 | +1 | 0 | +0.5 | 최저위험 — 호환성 최고 |
| 밤 조각 | 0 | 0 | +0.5 | 0 | 0 | 가장 중립적 혼입물 |
| 밤가루 | −0.5 | 0 | +1 | 0 | +0.5 | **무글루텐 — 20~25% 하드캡** |

### 18-5. 다중 재료 상위 레시피 로드맵 (Phase 10, 전부 URL 실증)

호밀+초코+체리 / 초코+체리 깜빠뉴 / 초코+동결건조딸기 포카치아 / 딸기+화이트초코 /
올리브+페타(+로즈마리) / 밤+무화과 바게트 / 밤+고다+로즈마리 / 밤+크랜베리 포카치아 등 10종 확보 —
"재료 2개 조합 해금"의 콘텐츠 뒷받침이 실존 레시피로 가능.

### 18-6. 파생 결정

- 판정표는 `CompatibilityRule[]` 데이터 파일로 전사(§8-2) — evidenceTier·sourceRef 포함.
- experimental 조합은 게임에서 "실험 굽기"로 노출 가능(재료 소비 0, 해금 없음, 힌트만) — 또는 v1은
  verified+conditional만 노출. **v1은 후자 추천**(콘텐츠 40행이면 충분).
- 팬케이크 계열에 "세이버리" 변형 축 추가 검토(올리브·파·허브) — KAB 실증 있음.

---

## 19. 현실 근거 카탈로그 (기준일 2026-08-24, 전 출처 직접 열람)

등급: A=정부·대학 Extension·동료심사 / B=King Arthur Baking(웨이백 경유 본문 확인) /
C=전문 제빵사 / D=커뮤니티. 안전 주장은 전부 A/B만 사용.

### 19-1. 검증 결과: **현 게임 상수가 이미 현실 정합**

| 비율 | 게임(constants.ts) 피크 | 현실 실측 | 판정 |
|---|---|---|---|
| 1:1:1 | 4.5~6h | KA 실측 4~6h(범위 4~8h, 25.5°C) [B] | ✅ 일치 |
| 1:2:2 | 7~9h | KA 실측 ~8h [B] | ✅ 일치 |
| 1:5:5 | 10~12h | The Perfect Loaf 루틴 10~12h(24°C) [C] | ✅ 일치 |

KA 스스로 "가이드일 뿐, 상태·온도로 변동"이라 명시 — 게임 UI도 **범위로 말한다**(§7-1 관찰 카드).

### 19-2. 게임에 안전하게 옮길 수 있는 것 (근거 확보)

- **온도 다이얼**: 효모 최적 27°C(상한 36°C), 유산균 32~33°C(상한 41°C) — 20~26°C 효모 우위,
  30°C+ 산미 우위 (Gänzle 1998, A). 창가=이르고 산미 가속인 현 설계와 정합.
- **hooch = 배고픔 게이지, 오염 아님**: 색은 위험도가 아니라 **방치 시간**(맑음→어둠→검정/껍질).
  검정도 "totally fine"(KA 제목 그대로). 젓기/따라내기 선택, 많고 어두울수록 회복 비용↑ [B].
- **폐기 기준**: 색 있는 반점·솜털(CSU: "colored and/or fuzzy" 사용 금지), 분홍·주황 줄무늬,
  부패취 = 폐기·용기 세척·재시작. **고수분 식품은 "부분만 걷어내기" 불가**(USDA FSIS) [A] —
  현 moldy 종착 규칙의 정확한 근거.
- **kahm**: CSU가 "whitish clumps = 안전한 효모, 곰팡이 아님"까지 확인 [A]. 게임 문구 상한 =
  "곰팡이는 아니에요" — "먹어도 됨" 단정 금지(무해 서술은 D급뿐).
- **회복 판정식**: 성공 = "급여 후 6~8h 내 2배" / 실패 = "하루 2회 × 3~4일 무반응 → 새로 시작" [B]
  — 현 부활 의식(2급여)과 방향 일치, 문구·관찰 카드에 그대로 쓸 수 있는 수치.
- **건조**: 얇게 펴 1~5일 건조, 밀폐 보관 시 사실상 무기한(10년 사례), 재수화 2~4일 [B] —
  flake의 현실 대응물. "오래된 조각 부활" 이벤트도 실화 기반.
- **냉장 실무**: 실온 하루 2회 ↔ 냉장 주 1회 [B, CSU A] — 현 ×0.08 설계와 정합. 꺼내면 12h 간격
  급여 1~2회+ 재활성화 — 부활 의식 아님, 가벼운 "깨우기"(§7-1 프리셋).
- **discard 페널티**: 안 버리면 부피 복리 폭증(확정) + 산 축적→효모 억제(초산 150mmol/L에서
  *C. milleri* 완전 억제, Gänzle A — 추론 층위 명시).
- **밀가루**: 통밀·호밀 = 발효 가속·산 상한 상승(방향 확정 B/C, **정량 배수는 TO VERIFY**) —
  §7-2의 ×1.15~1.25는 튜닝 가설로 유지.
- **점탄성 정성 서술**: "천천히 늘고 일부만 되돌아온다"(creep-recovery), windowpane(얇게 늘여도
  안 찢어짐 — 산도가 글루텐 분해 시 찢어짐) — §4 촉감 목표의 문헌 뒷받침. 정량 시간척도는 삭제.

### 19-3. 게임에 옮기면 안 되는 것 (금지 목록)

1. "정답 시간" 고정 수치 — 전 출처가 변동 명시.
2. **맛보기·시식 메커닉** — CDC "생반죽·배터 금지" + CSU "생스타터 맛보기 금지" 정면 위반.
   판정은 기포·냄새·팽창·농도로(CSU 제시 목록 그대로).
3. 아이·펫이 반죽 갖고 노는 연출 — CDC 명시 금지(공예 반죽 포함).
4. **곰팡이 걷어내고 계속 키우기** — FSIS 고수분 원칙 위반. 현 moldy 종착 유지가 옳다.
   (반점·확산 단계의 급여 회복은 게임 추상화로만 명시 — §7-3.)
5. hooch를 오염 신호로 묘사 — 정반대.
6. 냉동 보관 세이브 — KA 단독 주장("죽인다")이라 논쟁 있음. 건조가 논쟁 없는 대안.

---

## 20. 가정 및 사용자 확인 항목 (최대 5)

1. **광고·IAP 도입 자체** — PRIVACY·STORE_LISTING "광고 없음·결제 없음" 공개 선언을 뒤집는
   제품 결정. 이 문서는 Phase 8~9로 후치·설계만 완료. **도입 여부는 사용자 확정 필요.**
2. ~~₩1,900 방식~~ → **확정됨(2026-08-24): 확률 공개형 랜덤** (§11-1). 잔여 확인: 영세사업자 예외
   해당 여부 전문가 검토(해당해도 표시는 유지).
3. **5단계 label 보상 → 각인/칭호 전환** — 이름은 처음부터 자유. 기존 보상의 의미 변경.
4. **무료 르방 슬롯 3개** — 상한·추가 슬롯 가격은 MONEY 단계에서.
5. **도감 전역 승격**(starterId 기록 병기) — 마이그레이션 1회로 확정해야 하는 구조 결정.

**추가 확정(2026-08-24)**: 재료는 **순수 컬렉팅** — sim(밥주기·발효·판정)에 영향 0.
밥주기 세분화(§7)는 프리셋·문구·관찰 카드 차원이며, §7-2 밀가루 축도 sim 최소 침습 옵션으로만
유지(구현 여부는 Phase 4에서 재확인).

## 21. 요약 — 먼저 검증 3 · 위험 결정 3 · 다음 단계 3

**먼저 검증할 것 3**: ① grab 채널 프로토(Motion Lab)에서 "당기고-멈추면-유지"가 실기기에서
목표 촉감을 내는지 ② v1→v2 마이그레이션 픽스처 왕복(순서 반전 포함) ③ BUG-1·BUG-2 수정 후
쇼케이스·탭 전환 회귀.
**가장 위험한 결정 3**: ① 광고·IAP 도입(공개 선언 반전 + 새 AAB + 방침 전면 개정) ② 저장 v2
(순서 폭탄 + 재정박 N배) ③ label 보상 의미 변경(기존 사용자 체감).
**승인 시 다음 단계 3**: ① Phase 0(문서 정리+버그) 착수 ② Motion Lab 하네스 구축(breadlab 패턴)
③ 카탈로그 46행 `CompatibilityRule` 데이터 전사.
